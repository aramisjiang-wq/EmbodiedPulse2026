# 技术实现方案：Follow 世界的前沿

**版本**: v1.0  
**创建日期**: 2025-12-19  
**用途**: 详细技术实现方案  
**目标**: 提供可执行的开发指南

---

## 📋 目录

1. [技术架构设计](#技术架构设计)
2. [数据库设计](#数据库设计)
3. [后端实现](#后端实现)
4. [前端实现](#前端实现)
5. [定时任务实现](#定时任务实现)
6. [开发步骤](#开发步骤)
7. [测试方案](#测试方案)
8. [部署方案](#部署方案)
9. [迭代计划](#迭代计划)

---

## 🏗️ 技术架构设计

### ⚠️ 重要说明：与现有系统的集成关系

**这是一个与现有功能深度集成的新功能，而非独立系统。**

#### 核心设计理念
1. **数据统一存储**: 研究机构抓取的论文**统一存储**在现有的 `papers` 表中
2. **功能复用**: 复用现有的论文展示、搜索、分类等功能
3. **数据扩展**: 通过扩展 `papers` 表字段来标识论文来源
4. **新增功能**: 新增机构管理和机构论文列表展示功能

#### 与现有系统的关系

```
┌─────────────────────────────────────────────────────────────┐
│                     现有系统（已存在）                         │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 论文表 (papers)                                       │   │
│  │ - 存储ArXiv论文（已有）                               │   │
│  │ - 存储机构论文（新增，统一存储）                        │   │
│  │ - 扩展字段：source_institute_id, source_url          │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 论文展示功能（复用）                                    │   │
│  │ - 首页论文列表（/）                                    │   │
│  │ - 论文搜索（/api/search）                             │   │
│  │ - 论文分类（33个分类）                                 │   │
│  │ - 论文统计（总数、新增数等）                            │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ 集成
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                     新功能（新增）                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 机构表 (research_institutes) - 新表                   │   │
│  │ - 存储机构基本信息                                     │   │
│  │ - 存储抓取配置                                         │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 抓取日志表 (fetch_logs) - 新表                        │   │
│  │ - 记录每次抓取的结果                                   │   │
│  │ - 记录错误信息                                         │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 机构论文展示（新页面）                                  │   │
│  │ - /research-institutes（机构列表）                     │   │
│  │ - /research-institutes/<id>/papers（机构论文列表）    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     前端展示层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ 机构列表页面  │  │ 机构论文列表  │  │ 现有论文页面  │ │
│  │ (新)         │  │ (新)         │  │ (复用)       │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    Flask API 层                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ /api/institutes│ │ /api/papers  │ │ /api/search  │ │
│  │ (新)         │  │ (复用)       │  │ (复用)       │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    业务逻辑层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ 机构管理服务  │  │ 论文管理服务  │  │ 抓取调度服务  │ │
│  │ (新)         │  │ (复用+扩展)  │  │ (新)         │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    数据抓取层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ AllenAI客户端 │  │ NVIDIA客户端 │  │ PI客户端     │ │
│  │ (新)         │  │ (新)         │  │ (新)         │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    数据存储层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ 机构表(新)    │  │ 论文表(扩展) │  │ 抓取日志表(新)│ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 数据流向说明

```
研究机构网站
    │
    ▼
[抓取客户端] (新)
    │
    ▼
[论文数据] (标准化)
    │
    ▼
[去重检查] (复用现有逻辑)
    │
    ▼
[papers表] (现有表，扩展字段)
    │
    ├───► [首页论文列表] (复用现有页面)
    ├───► [论文搜索] (复用现有功能)
    ├───► [论文分类] (复用现有功能)
    └───► [机构论文列表] (新页面)
```

### 模块划分

#### 1. 数据模型层 (`research_institute_models.py`)
- `ResearchInstitute`: 研究机构模型
- `FetchLog`: 抓取日志模型

#### 2. 客户端层 (`research_institute_clients/`)
- `base_client.py`: 基础客户端抽象类
- `allenai_client.py`: AllenAI客户端
- `nvidia_research_client.py`: NVIDIA Research客户端
- `physical_intelligence_client.py`: Physical Intelligence客户端

#### 3. 服务层 (`research_institute_service.py`)
- 机构管理服务
- 论文抓取服务
- 数据去重服务

#### 4. API路由层 (`app.py` 扩展)
- `/research-institutes`: 机构列表
- `/research-institutes/<id>/papers`: 机构论文列表
- `/research-institutes/stats`: 统计数据

#### 5. 定时任务层 (`scheduler_utils.py` 扩展)
- 定时抓取任务
- 错误重试机制

---

## 🗄️ 数据库设计

### ⚠️ 数据库设计说明

**这是一个扩展性设计，而非独立数据库：**

1. **新增表**（2个）:
   - `research_institutes`: 存储机构信息
   - `fetch_logs`: 存储抓取日志

2. **扩展现有表**（1个）:
   - `papers`: 添加3个字段标识论文来源
   - **重要**: 机构论文与ArXiv论文统一存储在 `papers` 表中

3. **数据统一性**:
   - 所有论文（无论来源）都在同一个 `papers` 表中
   - 可以通过 `source_institute_id` 字段区分论文来源
   - 如果 `source_institute_id` 为 NULL，说明是ArXiv论文（现有数据）

### 1. research_institutes 表（新表）

```sql
CREATE TABLE research_institutes (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL UNIQUE,  -- 机构名称，如 "AllenAI"
    slug VARCHAR(100) NOT NULL UNIQUE,  -- URL友好标识，如 "allenai"
    website_url TEXT,  -- 官网URL
    papers_url TEXT NOT NULL,  -- 论文列表URL
    logo_url TEXT,  -- Logo URL
    description TEXT,  -- 机构简介
    fetch_method VARCHAR(50) NOT NULL DEFAULT 'crawler',  -- 抓取方式：crawler/api/rss
    fetch_config JSONB,  -- 抓取配置（API key、CSS选择器等）
    is_active BOOLEAN DEFAULT TRUE,  -- 是否启用
    last_fetch_at TIMESTAMP,  -- 最后抓取时间
    fetch_interval_hours INTEGER DEFAULT 24,  -- 抓取间隔（小时）
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_research_institutes_active ON research_institutes(is_active);
CREATE INDEX idx_research_institutes_slug ON research_institutes(slug);
```

### 2. papers 表扩展（扩展现有表）

**说明**: 这是对现有 `papers` 表的扩展，不是新建表。

```sql
-- 添加新字段（用于标识论文来源）
ALTER TABLE papers ADD COLUMN source_institute_id INTEGER REFERENCES research_institutes(id);
ALTER TABLE papers ADD COLUMN source_url TEXT;  -- 原始论文页面URL
ALTER TABLE papers ADD COLUMN fetch_method VARCHAR(50);  -- 抓取方式

-- 索引
CREATE INDEX idx_papers_source_institute ON papers(source_institute_id);
```

**字段说明**:
- `source_institute_id`: 
  - `NULL` = ArXiv论文（现有数据，保持兼容）
  - `有值` = 研究机构论文（新数据）
- `source_url`: 论文的原始页面URL（机构网站上的链接）
- `fetch_method`: 抓取方式（'crawler'/'api'/'rss'）

**数据兼容性**:
- 现有ArXiv论文的 `source_institute_id` 为 `NULL`，不影响现有功能
- 新抓取的机构论文会设置 `source_institute_id`
- 所有论文都可以通过现有的 `/api/papers` 和 `/api/search` API访问

### 3. fetch_logs 表（新表）

```sql
CREATE TABLE fetch_logs (
    id SERIAL PRIMARY KEY,
    institute_id INTEGER REFERENCES research_institutes(id),
    fetch_type VARCHAR(50) NOT NULL,  -- full/incremental
    status VARCHAR(50) NOT NULL,  -- success/failed/partial
    papers_found INTEGER DEFAULT 0,  -- 发现论文数
    papers_new INTEGER DEFAULT 0,  -- 新增论文数
    papers_updated INTEGER DEFAULT 0,  -- 更新论文数
    papers_skipped INTEGER DEFAULT 0,  -- 跳过论文数（重复）
    error_message TEXT,  -- 错误信息
    started_at TIMESTAMP NOT NULL,
    completed_at TIMESTAMP,
    duration_seconds INTEGER
);

-- 索引
CREATE INDEX idx_fetch_logs_institute ON fetch_logs(institute_id);
CREATE INDEX idx_fetch_logs_status ON fetch_logs(status);
CREATE INDEX idx_fetch_logs_started_at ON fetch_logs(started_at);
```

### 4. 数据库迁移脚本

创建 `migrate_add_research_institutes.py`:

```python
"""
数据库迁移：添加研究机构追踪功能
"""
from database import get_engine
from sqlalchemy import text
import logging

logger = logging.getLogger(__name__)

def migrate():
    """执行迁移"""
    engine = get_engine()
    
    with engine.connect() as conn:
        # 1. 创建 research_institutes 表
        conn.execute(text("""
            CREATE TABLE IF NOT EXISTS research_institutes (
                id SERIAL PRIMARY KEY,
                name VARCHAR(255) NOT NULL UNIQUE,
                slug VARCHAR(100) NOT NULL UNIQUE,
                website_url TEXT,
                papers_url TEXT NOT NULL,
                logo_url TEXT,
                description TEXT,
                fetch_method VARCHAR(50) NOT NULL DEFAULT 'crawler',
                fetch_config JSONB,
                is_active BOOLEAN DEFAULT TRUE,
                last_fetch_at TIMESTAMP,
                fetch_interval_hours INTEGER DEFAULT 24,
                created_at TIMESTAMP DEFAULT NOW(),
                updated_at TIMESTAMP DEFAULT NOW()
            )
        """))
        
        # 2. 创建索引
        conn.execute(text("""
            CREATE INDEX IF NOT EXISTS idx_research_institutes_active 
            ON research_institutes(is_active)
        """))
        
        conn.execute(text("""
            CREATE INDEX IF NOT EXISTS idx_research_institutes_slug 
            ON research_institutes(slug)
        """))
        
        # 3. 扩展 papers 表
        try:
            conn.execute(text("""
                ALTER TABLE papers 
                ADD COLUMN IF NOT EXISTS source_institute_id INTEGER 
                REFERENCES research_institutes(id)
            """))
        except Exception as e:
            logger.warning(f"添加 source_institute_id 字段失败（可能已存在）: {e}")
        
        try:
            conn.execute(text("""
                ALTER TABLE papers 
                ADD COLUMN IF NOT EXISTS source_url TEXT
            """))
        except Exception as e:
            logger.warning(f"添加 source_url 字段失败（可能已存在）: {e}")
        
        try:
            conn.execute(text("""
                ALTER TABLE papers 
                ADD COLUMN IF NOT EXISTS fetch_method VARCHAR(50)
            """))
        except Exception as e:
            logger.warning(f"添加 fetch_method 字段失败（可能已存在）: {e}")
        
        # 4. 创建 papers 表索引
        try:
            conn.execute(text("""
                CREATE INDEX IF NOT EXISTS idx_papers_source_institute 
                ON papers(source_institute_id)
            """))
        except Exception as e:
            logger.warning(f"创建索引失败: {e}")
        
        # 5. 创建 fetch_logs 表
        conn.execute(text("""
            CREATE TABLE IF NOT EXISTS fetch_logs (
                id SERIAL PRIMARY KEY,
                institute_id INTEGER REFERENCES research_institutes(id),
                fetch_type VARCHAR(50) NOT NULL,
                status VARCHAR(50) NOT NULL,
                papers_found INTEGER DEFAULT 0,
                papers_new INTEGER DEFAULT 0,
                papers_updated INTEGER DEFAULT 0,
                papers_skipped INTEGER DEFAULT 0,
                error_message TEXT,
                started_at TIMESTAMP NOT NULL,
                completed_at TIMESTAMP,
                duration_seconds INTEGER
            )
        """))
        
        # 6. 创建 fetch_logs 索引
        conn.execute(text("""
            CREATE INDEX IF NOT EXISTS idx_fetch_logs_institute 
            ON fetch_logs(institute_id)
        """))
        
        conn.execute(text("""
            CREATE INDEX IF NOT EXISTS idx_fetch_logs_status 
            ON fetch_logs(status)
        """))
        
        conn.execute(text("""
            CREATE INDEX IF NOT EXISTS idx_fetch_logs_started_at 
            ON fetch_logs(started_at)
        """))
        
        conn.commit()
        logger.info("数据库迁移完成")

if __name__ == '__main__':
    migrate()
```

---

## 🔧 后端实现

### 1. 数据模型 (`research_institute_models.py`)

```python
"""
研究机构数据模型
"""
from sqlalchemy import Column, String, Text, DateTime, Integer, Boolean, JSON
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker, relationship
from datetime import datetime
from database import get_engine

Base = declarative_base()

class ResearchInstitute(Base):
    """研究机构模型"""
    __tablename__ = 'research_institutes'
    
    id = Column(Integer, primary_key=True)
    name = Column(String(255), nullable=False, unique=True)
    slug = Column(String(100), nullable=False, unique=True)
    website_url = Column(Text)
    papers_url = Column(Text, nullable=False)
    logo_url = Column(Text)
    description = Column(Text)
    fetch_method = Column(String(50), nullable=False, default='crawler')
    fetch_config = Column(JSON)
    is_active = Column(Boolean, default=True)
    last_fetch_at = Column(DateTime)
    fetch_interval_hours = Column(Integer, default=24)
    created_at = Column(DateTime, default=datetime.now)
    updated_at = Column(DateTime, default=datetime.now, onupdate=datetime.now)
    
    def to_dict(self):
        """转换为字典"""
        return {
            'id': self.id,
            'name': self.name,
            'slug': self.slug,
            'website_url': self.website_url,
            'papers_url': self.papers_url,
            'logo_url': self.logo_url,
            'description': self.description,
            'fetch_method': self.fetch_method,
            'is_active': self.is_active,
            'last_fetch_at': self.last_fetch_at.isoformat() if self.last_fetch_at else None,
            'fetch_interval_hours': self.fetch_interval_hours,
            'papers_count': self.papers_count if hasattr(self, 'papers_count') else 0
        }

class FetchLog(Base):
    """抓取日志模型"""
    __tablename__ = 'fetch_logs'
    
    id = Column(Integer, primary_key=True)
    institute_id = Column(Integer, nullable=False)
    fetch_type = Column(String(50), nullable=False)  # full/incremental
    status = Column(String(50), nullable=False)  # success/failed/partial
    papers_found = Column(Integer, default=0)
    papers_new = Column(Integer, default=0)
    papers_updated = Column(Integer, default=0)
    papers_skipped = Column(Integer, default=0)
    error_message = Column(Text)
    started_at = Column(DateTime, nullable=False, default=datetime.now)
    completed_at = Column(DateTime)
    duration_seconds = Column(Integer)
    
    def to_dict(self):
        """转换为字典"""
        return {
            'id': self.id,
            'institute_id': self.institute_id,
            'fetch_type': self.fetch_type,
            'status': self.status,
            'papers_found': self.papers_found,
            'papers_new': self.papers_new,
            'papers_updated': self.papers_updated,
            'papers_skipped': self.papers_skipped,
            'error_message': self.error_message,
            'started_at': self.started_at.isoformat() if self.started_at else None,
            'completed_at': self.completed_at.isoformat() if self.completed_at else None,
            'duration_seconds': self.duration_seconds
        }

def get_research_institute_session():
    """获取数据库会话"""
    engine = get_engine()
    Session = sessionmaker(bind=engine)
    return Session()
```

### 2. 基础客户端 (`research_institute_clients/base_client.py`)

```python
"""
研究机构客户端基类
"""
from abc import ABC, abstractmethod
import logging
from typing import List, Dict
from datetime import datetime

logger = logging.getLogger(__name__)

class BaseResearchInstituteClient(ABC):
    """研究机构客户端基类"""
    
    def __init__(self, institute_config: Dict):
        """
        初始化客户端
        
        Args:
            institute_config: 机构配置字典，包含name, papers_url, fetch_config等
        """
        self.name = institute_config.get('name')
        self.papers_url = institute_config.get('papers_url')
        self.fetch_config = institute_config.get('fetch_config', {})
        self.timeout = self.fetch_config.get('timeout', 30)
        self.retry_times = self.fetch_config.get('retry_times', 3)
        self.retry_delay = self.fetch_config.get('retry_delay', 5)
    
    @abstractmethod
    def fetch_papers(self, max_papers: int = None) -> List[Dict]:
        """
        抓取论文列表
        
        Args:
            max_papers: 最大抓取数量，None表示抓取所有
        
        Returns:
            论文列表，每个论文包含：
            - title: 标题
            - authors: 作者列表
            - publish_year: 发表年份
            - venue: 发表场所
            - paper_url: 论文链接
            - pdf_url: PDF链接（如果有）
            - abstract: 摘要（如果有）
            - source_url: 原始页面URL
        """
        pass
    
    def normalize_paper_data(self, raw_data: Dict) -> Dict:
        """
        标准化论文数据
        
        Args:
            raw_data: 原始数据
        
        Returns:
            标准化后的数据
        """
        return {
            'title': raw_data.get('title', '').strip(),
            'authors': raw_data.get('authors', []),
            'publish_year': raw_data.get('publish_year'),
            'venue': raw_data.get('venue', ''),
            'paper_url': raw_data.get('paper_url', ''),
            'pdf_url': raw_data.get('pdf_url', ''),
            'abstract': raw_data.get('abstract', ''),
            'source_url': raw_data.get('source_url', '')
        }
    
    def validate_paper_data(self, paper_data: Dict) -> bool:
        """
        验证论文数据是否完整
        
        Args:
            paper_data: 论文数据
        
        Returns:
            是否有效
        """
        required_fields = ['title', 'paper_url']
        return all(paper_data.get(field) for field in required_fields)
```

### 3. AllenAI客户端 (`research_institute_clients/allenai_client.py`)

```python
"""
AllenAI客户端
"""
import requests
from bs4 import BeautifulSoup
import time
import logging
from typing import List, Dict
from research_institute_clients.base_client import BaseResearchInstituteClient

logger = logging.getLogger(__name__)

class AllenAIClient(BaseResearchInstituteClient):
    """AllenAI客户端"""
    
    def fetch_papers(self, max_papers: int = None) -> List[Dict]:
        """
        抓取AllenAI论文列表
        
        实现思路：
        1. 访问 https://allenai.org/papers
        2. 解析HTML，提取论文信息
        3. 返回标准化数据
        """
        papers = []
        
        try:
            # 设置请求头
            headers = {
                'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'
            }
            
            # 发送请求
            response = requests.get(
                self.papers_url,
                headers=headers,
                timeout=self.timeout
            )
            response.raise_for_status()
            
            # 解析HTML
            soup = BeautifulSoup(response.text, 'html.parser')
            
            # 根据实际HTML结构提取论文
            # 这里需要根据实际网站结构调整选择器
            paper_elements = soup.select('.paper-item')  # 示例选择器
            
            for element in paper_elements:
                if max_papers and len(papers) >= max_papers:
                    break
                
                try:
                    paper_data = self._parse_paper_element(element)
                    if self.validate_paper_data(paper_data):
                        papers.append(self.normalize_paper_data(paper_data))
                except Exception as e:
                    logger.warning(f"解析论文元素失败: {e}")
                    continue
            
            logger.info(f"AllenAI抓取完成，共{len(papers)}篇论文")
            
        except Exception as e:
            logger.error(f"AllenAI抓取失败: {e}")
            raise
        
        return papers
    
    def _parse_paper_element(self, element) -> Dict:
        """
        解析单个论文元素
        
        Args:
            element: BeautifulSoup元素
        
        Returns:
            论文数据字典
        """
        # 根据实际HTML结构调整
        title_elem = element.select_one('.paper-title')
        title = title_elem.get_text(strip=True) if title_elem else ''
        
        authors_elem = element.select('.paper-authors')
        authors = [a.get_text(strip=True) for a in authors_elem] if authors_elem else []
        
        year_elem = element.select_one('.paper-year')
        year = int(year_elem.get_text(strip=True)) if year_elem else None
        
        link_elem = element.select_one('a.paper-link')
        paper_url = link_elem.get('href', '') if link_elem else ''
        if paper_url and not paper_url.startswith('http'):
            paper_url = f"https://allenai.org{paper_url}"
        
        return {
            'title': title,
            'authors': authors,
            'publish_year': year,
            'venue': '',
            'paper_url': paper_url,
            'pdf_url': '',
            'abstract': '',
            'source_url': self.papers_url
        }
```

### 4. 论文抓取服务 (`research_institute_service.py`)

```python
"""
研究机构论文抓取服务
"""
import logging
from datetime import datetime
from typing import List, Dict, Tuple
from research_institute_models import get_research_institute_session, ResearchInstitute, FetchLog
from models import get_session, Paper
from save_paper_to_db import save_paper_to_db
from utils import is_duplicate_title
from research_institute_clients.allenai_client import AllenAIClient
from research_institute_clients.nvidia_research_client import NVIDIAResearchClient
from research_institute_clients.physical_intelligence_client import PhysicalIntelligenceClient

logger = logging.getLogger(__name__)

# 客户端映射
CLIENT_MAP = {
    'allenai': AllenAIClient,
    'nvidia_research': NVIDIAResearchClient,
    'physical_intelligence': PhysicalIntelligenceClient
}

def fetch_institute_papers(institute_id: int, max_papers: int = None) -> Dict:
    """
    抓取指定机构的论文
    
    Args:
        institute_id: 机构ID
        max_papers: 最大抓取数量
    
    Returns:
        抓取结果字典
    """
    session = get_research_institute_session()
    paper_session = get_session()
    
    try:
        # 获取机构信息
        institute = session.query(ResearchInstitute).filter_by(id=institute_id).first()
        if not institute:
            return {'success': False, 'error': '机构不存在'}
        
        if not institute.is_active:
            return {'success': False, 'error': '机构未启用'}
        
        # 创建抓取日志
        fetch_log = FetchLog(
            institute_id=institute_id,
            fetch_type='incremental',
            status='running',
            started_at=datetime.now()
        )
        session.add(fetch_log)
        session.commit()
        
        # 创建客户端
        client_class = CLIENT_MAP.get(institute.slug)
        if not client_class:
            fetch_log.status = 'failed'
            fetch_log.error_message = f'不支持的机构类型: {institute.slug}'
            fetch_log.completed_at = datetime.now()
            session.commit()
            return {'success': False, 'error': fetch_log.error_message}
        
        client = client_class({
            'name': institute.name,
            'papers_url': institute.papers_url,
            'fetch_config': institute.fetch_config or {}
        })
        
        # 抓取论文
        papers = client.fetch_papers(max_papers=max_papers)
        fetch_log.papers_found = len(papers)
        
        # 保存论文
        papers_new = 0
        papers_updated = 0
        papers_skipped = 0
        
        for paper_data in papers:
            try:
                # 检查是否已存在（基于标题相似度）
                existing_paper = paper_session.query(Paper).filter_by(
                    title=paper_data['title']
                ).first()
                
                if existing_paper:
                    # 如果已存在，更新来源机构信息
                    if not existing_paper.source_institute_id:
                        existing_paper.source_institute_id = institute_id
                        existing_paper.source_url = paper_data.get('source_url')
                        existing_paper.fetch_method = institute.fetch_method
                        papers_updated += 1
                    else:
                        papers_skipped += 1
                    continue
                
                # 检查标题相似度
                similar_papers = paper_session.query(Paper).all()
                is_duplicate = False
                for existing in similar_papers:
                    if is_duplicate_title(paper_data['title'], existing.title):
                        is_duplicate = True
                        break
                
                if is_duplicate:
                    papers_skipped += 1
                    continue
                
                # 保存新论文
                paper_dict = {
                    'id': f"{institute.slug}_{paper_data['title'][:50]}",  # 生成唯一ID
                    'title': paper_data['title'],
                    'authors': ', '.join(paper_data.get('authors', [])),
                    'publish_date': datetime(paper_data.get('publish_year', 2024), 1, 1).date() if paper_data.get('publish_year') else None,
                    'pdf_url': paper_data.get('pdf_url', ''),
                    'code_url': '',
                    'abstract': paper_data.get('abstract', ''),
                    'category': 'Other',  # 默认分类，后续可以自动分类
                    'source_institute_id': institute_id,
                    'source_url': paper_data.get('source_url', ''),
                    'fetch_method': institute.fetch_method
                }
                
                success, action = save_paper_to_db(
                    paper_dict,
                    category='Other',
                    enable_title_dedup=False  # 已经在上面处理了
                )
                
                if success and action == 'created':
                    papers_new += 1
                elif success and action == 'updated':
                    papers_updated += 1
                else:
                    papers_skipped += 1
                    
            except Exception as e:
                logger.error(f"保存论文失败: {e}")
                papers_skipped += 1
                continue
        
        # 更新抓取日志
        fetch_log.status = 'success'
        fetch_log.papers_new = papers_new
        fetch_log.papers_updated = papers_updated
        fetch_log.papers_skipped = papers_skipped
        fetch_log.completed_at = datetime.now()
        fetch_log.duration_seconds = int((fetch_log.completed_at - fetch_log.started_at).total_seconds())
        
        # 更新机构最后抓取时间
        institute.last_fetch_at = datetime.now()
        
        session.commit()
        paper_session.commit()
        
        return {
            'success': True,
            'papers_found': len(papers),
            'papers_new': papers_new,
            'papers_updated': papers_updated,
            'papers_skipped': papers_skipped
        }
        
    except Exception as e:
        logger.error(f"抓取机构论文失败: {e}")
        if 'fetch_log' in locals():
            fetch_log.status = 'failed'
            fetch_log.error_message = str(e)
            fetch_log.completed_at = datetime.now()
            session.commit()
        return {'success': False, 'error': str(e)}
    
    finally:
        session.close()
        paper_session.close()

def fetch_all_institutes(max_papers_per_institute: int = None) -> Dict:
    """
    抓取所有启用机构的论文
    
    Args:
        max_papers_per_institute: 每个机构最大抓取数量
    
    Returns:
        总体抓取结果
    """
    session = get_research_institute_session()
    
    try:
        institutes = session.query(ResearchInstitute).filter_by(is_active=True).all()
        
        results = {
            'total_institutes': len(institutes),
            'success_count': 0,
            'failed_count': 0,
            'details': []
        }
        
        for institute in institutes:
            try:
                result = fetch_institute_papers(institute.id, max_papers=max_papers_per_institute)
                results['details'].append({
                    'institute_id': institute.id,
                    'institute_name': institute.name,
                    'result': result
                })
                
                if result.get('success'):
                    results['success_count'] += 1
                else:
                    results['failed_count'] += 1
                    
            except Exception as e:
                logger.error(f"抓取机构 {institute.name} 失败: {e}")
                results['failed_count'] += 1
                results['details'].append({
                    'institute_id': institute.id,
                    'institute_name': institute.name,
                    'result': {'success': False, 'error': str(e)}
                })
        
        return results
        
    finally:
        session.close()
```

### 5. API路由扩展 (`app.py`)

```python
# 在 app.py 中添加以下路由

from research_institute_models import get_research_institute_session, ResearchInstitute, FetchLog
from research_institute_service import fetch_institute_papers, fetch_all_institutes
from models import get_session, Paper
from sqlalchemy import func

@app.route('/research-institutes')
def research_institutes():
    """研究机构列表页面"""
    session = get_research_institute_session()
    try:
        institutes = session.query(ResearchInstitute).filter_by(is_active=True).all()
        
        # 统计每个机构的论文数
        paper_session = get_session()
        for institute in institutes:
            count = paper_session.query(Paper).filter_by(
                source_institute_id=institute.id
            ).count()
            institute.papers_count = count
        paper_session.close()
        
        return render_template('research_institutes.html', institutes=institutes)
    finally:
        session.close()

@app.route('/api/research-institutes')
def api_research_institutes():
    """研究机构列表API"""
    session = get_research_institute_session()
    try:
        institutes = session.query(ResearchInstitute).filter_by(is_active=True).all()
        
        # 统计每个机构的论文数
        paper_session = get_session()
        result = []
        for institute in institutes:
            count = paper_session.query(Paper).filter_by(
                source_institute_id=institute.id
            ).count()
            result.append({
                **institute.to_dict(),
                'papers_count': count
            })
        paper_session.close()
        
        return jsonify(result)
    finally:
        session.close()

@app.route('/research-institutes/<int:institute_id>/papers')
def institute_papers(institute_id):
    """机构论文列表页面"""
    session = get_research_institute_session()
    paper_session = get_session()
    
    try:
        institute = session.query(ResearchInstitute).filter_by(id=institute_id).first()
        if not institute:
            return "机构不存在", 404
        
        # 获取论文列表
        papers = paper_session.query(Paper).filter_by(
            source_institute_id=institute_id
        ).order_by(Paper.publish_date.desc()).limit(100).all()
        
        return render_template('institute_papers.html', 
                             institute=institute, 
                             papers=papers)
    finally:
        session.close()
        paper_session.close()

@app.route('/api/research-institutes/<int:institute_id>/papers')
def api_institute_papers(institute_id):
    """机构论文列表API"""
    paper_session = get_session()
    
    try:
        page = request.args.get('page', 1, type=int)
        per_page = request.args.get('per_page', 20, type=int)
        
        papers = paper_session.query(Paper).filter_by(
            source_institute_id=institute_id
        ).order_by(Paper.publish_date.desc())
        
        total = papers.count()
        papers = papers.offset((page - 1) * per_page).limit(per_page).all()
        
        return jsonify({
            'total': total,
            'page': page,
            'per_page': per_page,
            'papers': [p.to_dict() for p in papers]
        })
    finally:
        paper_session.close()

@app.route('/api/research-institutes/stats')
def api_research_institutes_stats():
    """研究机构统计数据API"""
    session = get_research_institute_session()
    paper_session = get_session()
    
    try:
        institutes = session.query(ResearchInstitute).filter_by(is_active=True).all()
        
        stats = []
        for institute in institutes:
            papers = paper_session.query(Paper).filter_by(
                source_institute_id=institute.id
            )
            
            total = papers.count()
            recent = papers.filter(
                Paper.publish_date >= datetime.now().date() - timedelta(days=30)
            ).count()
            
            stats.append({
                'institute_id': institute.id,
                'institute_name': institute.name,
                'total_papers': total,
                'recent_papers': recent
            })
        
        return jsonify(stats)
    finally:
        session.close()
        paper_session.close()

@app.route('/api/research-institutes/<int:institute_id>/fetch', methods=['POST'])
@admin_required  # 需要管理员权限
def api_fetch_institute_papers(institute_id):
    """手动触发机构论文抓取"""
    result = fetch_institute_papers(institute_id)
    return jsonify(result)
```

---

## 🎨 前端实现

### 1. 机构列表页面 (`templates/research_institutes.html`)

```html
{% extends "base.html" %}

{% block title %}研究机构追踪 - Embodied Pulse{% endblock %}

{% block content %}
<div class="container mt-4">
    <h1>Follow 世界的前沿</h1>
    <p class="lead">追踪具身智能领域顶级研究机构的最新论文</p>
    
    <div class="row mt-4">
        {% for institute in institutes %}
        <div class="col-md-4 mb-4">
            <div class="card">
                {% if institute.logo_url %}
                <img src="{{ institute.logo_url }}" class="card-img-top" alt="{{ institute.name }}">
                {% endif %}
                <div class="card-body">
                    <h5 class="card-title">{{ institute.name }}</h5>
                    <p class="card-text">{{ institute.description or '' }}</p>
                    <p class="text-muted">
                        <small>论文数: {{ institute.papers_count or 0 }}</small><br>
                        <small>最后更新: {{ institute.last_fetch_at.strftime('%Y-%m-%d %H:%M') if institute.last_fetch_at else '未更新' }}</small>
                    </p>
                    <a href="/research-institutes/{{ institute.id }}/papers" class="btn btn-primary">查看论文</a>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
```

### 2. 机构论文列表页面 (`templates/institute_papers.html`)

```html
{% extends "base.html" %}

{% block title %}{{ institute.name }} - 论文列表{% endblock %}

{% block content %}
<div class="container mt-4">
    <h1>{{ institute.name }}</h1>
    <p class="lead">{{ institute.description or '' }}</p>
    
    <div class="mt-4">
        <h2>论文列表</h2>
        <div id="papers-list">
            {% for paper in papers %}
            <div class="card mb-3">
                <div class="card-body">
                    <h5 class="card-title">
                        <a href="{{ paper.paper_url or paper.pdf_url }}" target="_blank">{{ paper.title }}</a>
                    </h5>
                    <p class="card-text">
                        <strong>作者:</strong> {{ paper.authors }}<br>
                        <strong>发表日期:</strong> {{ paper.publish_date.strftime('%Y-%m-%d') if paper.publish_date else '未知' }}<br>
                        {% if paper.venue %}
                        <strong>发表场所:</strong> {{ paper.venue }}<br>
                        {% endif %}
                        {% if paper.citation_count %}
                        <strong>引用数:</strong> {{ paper.citation_count }}<br>
                        {% endif %}
                    </p>
                    {% if paper.abstract %}
                    <p class="card-text"><small class="text-muted">{{ paper.abstract[:200] }}...</small></p>
                    {% endif %}
                    <div class="mt-2">
                        {% if paper.pdf_url %}
                        <a href="{{ paper.pdf_url }}" class="btn btn-sm btn-primary" target="_blank">PDF</a>
                        {% endif %}
                        {% if paper.code_url %}
                        <a href="{{ paper.code_url }}" class="btn btn-sm btn-secondary" target="_blank">Code</a>
                        {% endif %}
                        <a href="{{ paper.source_url }}" class="btn btn-sm btn-info" target="_blank">原始页面</a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}
```

---

## ⏰ 定时任务实现

### 扩展 `scheduler_utils.py`

```python
# 在 scheduler_utils.py 中添加

from research_institute_service import fetch_all_institutes
import logging

logger = logging.getLogger(__name__)

def schedule_research_institute_fetch():
    """定时抓取研究机构论文"""
    try:
        logger.info("开始定时抓取研究机构论文")
        result = fetch_all_institutes(max_papers_per_institute=50)  # 每次最多抓取50篇
        
        logger.info(f"抓取完成: 成功{result['success_count']}个机构, 失败{result['failed_count']}个机构")
        
        # 记录详细结果
        for detail in result['details']:
            if detail['result'].get('success'):
                logger.info(f"{detail['institute_name']}: 新增{detail['result'].get('papers_new', 0)}篇, "
                          f"更新{detail['result'].get('papers_updated', 0)}篇")
            else:
                logger.error(f"{detail['institute_name']}: 抓取失败 - {detail['result'].get('error')}")
                
    except Exception as e:
        logger.error(f"定时抓取研究机构论文失败: {e}")

# 在初始化调度器时添加任务
# scheduler.add_job(
#     schedule_research_institute_fetch,
#     'cron',
#     hour=2,  # 每天凌晨2点执行
#     id='fetch_research_institutes'
# )
```

---

## 📝 开发步骤

### 阶段1: 数据库和模型（1-2天）

1. **创建数据库迁移脚本**
   - [ ] 创建 `migrate_add_research_institutes.py`
   - [ ] 执行迁移，创建表结构
   - [ ] 验证表结构

2. **创建数据模型**
   - [ ] 创建 `research_institute_models.py`
   - [ ] 实现 `ResearchInstitute` 模型
   - [ ] 实现 `FetchLog` 模型
   - [ ] 测试模型功能

3. **初始化机构数据**
   - [ ] 创建初始化脚本，插入3个机构的基础数据
   - [ ] 验证数据

### 阶段2: 客户端实现（3-5天）

1. **实现基础客户端**
   - [ ] 创建 `research_institute_clients/base_client.py`
   - [ ] 定义抽象接口

2. **实现AllenAI客户端**
   - [ ] 分析网站结构
   - [ ] 实现HTML解析
   - [ ] 测试抓取功能

3. **实现NVIDIA Research客户端**
   - [ ] 分析网站结构
   - [ ] 实现抓取逻辑
   - [ ] 测试抓取功能

4. **实现Physical Intelligence客户端**
   - [ ] 分析网站结构
   - [ ] 实现抓取逻辑
   - [ ] 测试抓取功能

### 阶段3: 服务层实现（2-3天）

1. **实现抓取服务**
   - [ ] 创建 `research_institute_service.py`
   - [ ] 实现 `fetch_institute_papers` 函数
   - [ ] 实现 `fetch_all_institutes` 函数
   - [ ] 实现去重逻辑
   - [ ] 测试服务功能

### 阶段4: API和前端（2-3天）

1. **实现API路由**
   - [ ] 在 `app.py` 中添加机构列表API
   - [ ] 添加机构论文列表API
   - [ ] 添加统计数据API
   - [ ] 添加手动抓取API

2. **实现前端页面**
   - [ ] 创建机构列表页面
   - [ ] 创建机构论文列表页面
   - [ ] 添加数据可视化（可选）

### 阶段5: 定时任务（1天）

1. **集成定时任务**
   - [ ] 在 `scheduler_utils.py` 中添加定时任务
   - [ ] 配置执行时间
   - [ ] 测试定时任务

### 阶段6: 测试和优化（2-3天）

1. **功能测试**
   - [ ] 测试抓取功能
   - [ ] 测试去重逻辑
   - [ ] 测试前端展示

2. **性能优化**
   - [ ] 优化抓取速度
   - [ ] 优化数据库查询
   - [ ] 添加缓存（如需要）

3. **错误处理**
   - [ ] 完善错误处理
   - [ ] 添加重试机制
   - [ ] 添加日志记录

---

## 🧪 测试方案

### 单元测试

```python
# tests/test_research_institute_client.py

import unittest
from research_institute_clients.allenai_client import AllenAIClient

class TestAllenAIClient(unittest.TestCase):
    def test_fetch_papers(self):
        client = AllenAIClient({
            'name': 'AllenAI',
            'papers_url': 'https://allenai.org/papers',
            'fetch_config': {}
        })
        papers = client.fetch_papers(max_papers=5)
        self.assertGreater(len(papers), 0)
        self.assertIn('title', papers[0])
        self.assertIn('paper_url', papers[0])
```

### 集成测试

```python
# tests/test_research_institute_service.py

import unittest
from research_institute_service import fetch_institute_papers

class TestResearchInstituteService(unittest.TestCase):
    def test_fetch_institute_papers(self):
        result = fetch_institute_papers(institute_id=1, max_papers=10)
        self.assertTrue(result.get('success'))
        self.assertGreater(result.get('papers_found', 0), 0)
```

---

## 🚀 部署方案

### 1. 数据库迁移

```bash
# 在服务器上执行
cd /path/to/embodied-pulse
source venv/bin/activate
python migrate_add_research_institutes.py
```

### 2. 代码部署

按照[代码管理规范与流程](../运维管理/代码管理规范与流程_完整版_20251219_v2.0.md)执行：
1. 本地开发完成
2. 推送到GitHub
3. 服务器拉取代码
4. 重启服务

### 3. 初始化机构数据

```python
# scripts/init_research_institutes.py

from research_institute_models import get_research_institute_session, ResearchInstitute

def init_institutes():
    session = get_research_institute_session()
    
    institutes = [
        {
            'name': 'AllenAI',
            'slug': 'allenai',
            'website_url': 'https://allenai.org',
            'papers_url': 'https://allenai.org/papers',
            'description': 'Allen Institute for AI',
            'fetch_method': 'crawler'
        },
        {
            'name': 'NVIDIA Research',
            'slug': 'nvidia_research',
            'website_url': 'https://research.nvidia.com',
            'papers_url': 'https://research.nvidia.com/publications',
            'description': 'NVIDIA Research',
            'fetch_method': 'crawler'
        },
        {
            'name': 'Physical Intelligence',
            'slug': 'physical_intelligence',
            'website_url': 'https://www.physicalintelligence.company',
            'papers_url': 'https://www.physicalintelligence.company',
            'description': 'Physical Intelligence Company',
            'fetch_method': 'crawler'
        }
    ]
    
    for data in institutes:
        existing = session.query(ResearchInstitute).filter_by(slug=data['slug']).first()
        if not existing:
            institute = ResearchInstitute(**data)
            session.add(institute)
    
    session.commit()
    session.close()

if __name__ == '__main__':
    init_institutes()
```

---

## 📅 迭代计划

### MVP版本（2周）
- ✅ 支持1个机构（AllenAI）
- ✅ 基础抓取功能
- ✅ 机构论文列表页面
- ✅ 定时任务

### v1.0版本（1周）
- ✅ 支持3个机构
- ✅ 完善去重逻辑
- ✅ 统计数据展示
- ✅ 错误处理和日志

### v1.1版本（后续）
- ⏳ 支持更多机构
- ⏳ API接口支持
- ⏳ RSS Feed支持
- ⏳ 邮件通知功能

---

## ✅ 总结

### 核心设计理念

**这是一个与现有功能深度集成的新功能，而非独立系统。**

#### 1. 数据统一存储
- ✅ 机构论文与ArXiv论文**统一存储**在 `papers` 表中
- ✅ 通过 `source_institute_id` 字段区分来源
- ✅ 现有ArXiv论文不受影响（`source_institute_id` 为 `NULL`）

#### 2. 功能复用
- ✅ 复用现有的论文展示页面（首页）
- ✅ 复用现有的论文搜索功能
- ✅ 复用现有的论文分类功能（33个分类）
- ✅ 复用现有的论文统计功能

#### 3. 新增功能
- ✅ 新增机构管理（机构表）
- ✅ 新增机构论文列表页面
- ✅ 新增机构论文抓取功能
- ✅ 新增抓取日志记录

#### 4. 数据流向
```
研究机构网站 → 抓取客户端 → 去重检查 → papers表 → 现有展示功能 + 新机构页面
```

### 技术要点
1. **数据获取**: 使用网页爬虫，后续可扩展API/RSS
2. **数据存储**: 扩展现有papers表，新增机构表和日志表
3. **去重策略**: 基于标题相似度和ID匹配（复用现有逻辑）
4. **定时任务**: 集成到现有调度系统

### 开发重点
1. **网站结构分析**: 需要仔细分析每个网站的HTML结构
2. **错误处理**: 完善的错误处理和重试机制
3. **数据质量**: 确保抓取的数据完整准确
4. **性能优化**: 避免对目标网站造成压力
5. **兼容性**: 确保不影响现有ArXiv论文功能

### 风险控制
1. **网站结构变化**: 需要监控和及时更新爬虫
2. **反爬虫机制**: 设置合理的请求频率和User-Agent
3. **数据去重**: 确保不重复存储相同论文（复用现有去重逻辑）
4. **数据兼容**: 确保现有ArXiv论文功能不受影响

---

**最后更新**: 2025-12-19  
**下一步**: 开始阶段1开发

