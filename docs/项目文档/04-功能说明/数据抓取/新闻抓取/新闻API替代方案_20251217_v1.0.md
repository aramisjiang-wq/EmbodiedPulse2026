# 新闻API替代方案

**文档创建时间**: 2025-12-09  
**最后更新时间**: 2025-12-09

---

## ⏰ APITube限制说明

### 限制解除时间
- **免费版限制**：通常每小时或每天有请求次数限制
- **解除时间**：可能需要等待 **1-24小时**（取决于具体限制类型）
- **建议**：等待几小时后重试，或考虑升级到付费版

---

## 🔄 替代API方案

### 方案1: NewsAPI.org ⭐ 推荐

**优点**：
- ✅ 免费版：500次请求/天
- ✅ 数据源丰富：70,000+新闻源
- ✅ 支持关键词搜索
- ✅ 支持日期范围过滤
- ✅ 文档完善，易于集成

**缺点**：
- ❌ 免费版有每日限制
- ❌ 需要注册获取API Key

**注册地址**：https://newsapi.org/

**示例代码**：
```python
import requests

API_KEY = "YOUR_NEWSAPI_KEY"
url = "https://newsapi.org/v2/everything"
params = {
    "q": "robot OR robotics OR embodied AI",
    "language": "en",
    "sortBy": "publishedAt",
    "pageSize": 50,
    "from": "2025-12-06",
    "to": "2025-12-09"
}
headers = {"X-Api-Key": API_KEY}
response = requests.get(url, params=params, headers=headers)
```

---

### 方案2: GNews API

**优点**：
- ✅ 免费版：100次请求/天
- ✅ 支持多语言
- ✅ 支持关键词搜索
- ✅ 无需信用卡

**缺点**：
- ❌ 免费版限制较严格
- ❌ 数据源相对较少

**注册地址**：https://gnews.io/

**示例代码**：
```python
import requests

API_KEY = "YOUR_GNEWS_KEY"
url = "https://gnews.io/api/v4/search"
params = {
    "q": "robot OR robotics OR embodied AI",
    "lang": "en",
    "max": 50,
    "apikey": API_KEY
}
response = requests.get(url, params=params)
```

---

### 方案3: Bing News Search API

**优点**：
- ✅ 免费版：1,000次请求/月
- ✅ 数据源丰富（Bing搜索）
- ✅ 支持高级搜索

**缺点**：
- ❌ 需要Azure账号
- ❌ 配置相对复杂

**注册地址**：https://azure.microsoft.com/services/cognitive-services/bing-news-search-api/

---

### 方案4: Google News RSS（免费）

**优点**：
- ✅ 完全免费
- ✅ 无需API Key
- ✅ 无请求限制

**缺点**：
- ❌ 需要解析RSS/HTML
- ❌ 数据格式不统一
- ❌ 可能被反爬虫机制限制

**示例代码**：
```python
import feedparser

url = "https://news.google.com/rss/search?q=robot+OR+robotics+OR+embodied+AI&hl=en&gl=US&ceid=US:en"
feed = feedparser.parse(url)
for entry in feed.entries:
    print(entry.title, entry.link)
```

---

### 方案5: 多API组合方案

**策略**：
- 主要使用 NewsAPI.org（免费版500次/天）
- 备用使用 GNews API（免费版100次/天）
- 如果都达到限制，使用 Google News RSS

**优点**：
- ✅ 提高可用性
- ✅ 分散请求压力
- ✅ 数据源更丰富

---

## 🔧 代码重构建议

### 抽象接口设计

创建统一的新闻API接口，便于切换：

```python
class NewsAPIClient:
    """新闻API客户端抽象基类"""
    def fetch_news(self, keywords, language, days_back):
        raise NotImplementedError

class NewsAPIClientImpl(NewsAPIClient):
    """NewsAPI.org实现"""
    def fetch_news(self, keywords, language, days_back):
        # 实现NewsAPI.org调用
        pass

class GNewsClientImpl(NewsAPIClient):
    """GNews API实现"""
    def fetch_news(self, keywords, language, days_back):
        # 实现GNews API调用
        pass
```

---

## 📊 方案对比

| API | 免费额度 | 易用性 | 数据质量 | 推荐度 |
|-----|---------|--------|---------|--------|
| **NewsAPI.org** | 500次/天 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **GNews API** | 100次/天 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Bing News** | 1000次/月 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Google RSS** | 无限制 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **APITube** | 未知 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 🎯 推荐方案

### 短期方案（立即可用）
1. **注册 NewsAPI.org**（最快，推荐）
   - 注册后立即获得500次/天
   - 代码改动最小
   - 数据质量好

2. **注册 GNews API**（备选）
   - 作为备用API
   - 100次/天，适合测试

### 长期方案
- 使用多API组合策略
- 实现API自动切换逻辑
- 监控API使用情况

---

## 📝 实施步骤

1. **选择API**：推荐 NewsAPI.org
2. **注册获取Key**：用户提供API Key
3. **修改代码**：替换 `apitube_client.py` 中的实现
4. **测试验证**：确保数据正常抓取
5. **更新文档**：记录新的API配置

---

**操作时间**: 2025-12-09  
**操作人员**: AI Assistant

