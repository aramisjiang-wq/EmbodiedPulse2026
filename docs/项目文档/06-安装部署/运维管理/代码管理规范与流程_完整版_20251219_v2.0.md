# 代码管理规范与流程 - 完整版

**版本**: v2.0  
**创建日期**: 2025-12-19  
**用途**: 规范本地开发、GitHub版本管理、服务器部署的完整流程  
**目标**: 确保迭代开发流程安全、可靠、清晰

---

## 📋 目录

1. [核心原则](#核心原则)
2. [分支管理策略](#分支管理策略)
3. [本地开发流程](#本地开发流程)
4. [GitHub版本管理流程](#github版本管理流程)
5. [服务器部署流程](#服务器部署流程)
6. [完整工作流示例](#完整工作流示例)
7. [安全检查清单](#安全检查清单)
8. [常见问题处理](#常见问题处理)
9. [回滚策略](#回滚策略)

---

## 🎯 核心原则

### 1. 永远保护生产环境
- ✅ **生产环境代码永远来自GitHub main分支**
- ✅ **禁止直接在生产服务器上修改代码**
- ✅ **所有修改必须先在本地测试，再推送到GitHub，最后部署到服务器**

### 2. 版本控制是唯一真相源
- ✅ **所有代码变更必须提交到GitHub**
- ✅ **提交信息必须清晰描述变更内容**
- ✅ **重要功能必须打标签（tag）**

### 3. 测试优先原则
- ✅ **本地测试通过才能推送**
- ✅ **关键功能需要额外验证**
- ✅ **数据库变更需要备份**

### 4. 可追溯性原则
- ✅ **每次部署都有对应的Git commit**
- ✅ **重要变更都有文档记录**
- ✅ **问题修复都有issue或文档追踪**

---

## 🌿 分支管理策略

### 分支类型

#### 1. main 分支（生产分支）
- **用途**: 生产环境代码，永远稳定可用
- **保护规则**:
  - 禁止直接push到main
  - 必须通过Pull Request合并
  - 必须通过代码审查（自己审查也可以）
- **部署**: 自动部署到生产服务器

#### 2. develop 分支（开发分支）
- **用途**: 日常开发的主分支
- **规则**:
  - 从main分支创建
  - 功能开发完成后合并到main
  - 可以随时部署到测试环境

#### 3. feature/* 分支（功能分支）
- **用途**: 新功能开发
- **命名**: `feature/功能名称_YYYYMMDD`
- **示例**: `feature/follow_research_institutes_20251219`
- **生命周期**:
  - 从develop创建
  - 开发完成后合并到develop
  - 合并后删除

#### 4. fix/* 分支（修复分支）
- **用途**: 紧急bug修复
- **命名**: `fix/问题描述_YYYYMMDD`
- **示例**: `fix/bilibili_data_missing_20251219`
- **生命周期**:
  - 可以从main或develop创建
  - 修复完成后合并到对应分支
  - 合并后删除

#### 5. hotfix/* 分支（热修复分支）
- **用途**: 生产环境紧急修复
- **命名**: `hotfix/问题描述_YYYYMMDD`
- **示例**: `hotfix/login_error_20251219`
- **生命周期**:
  - 从main创建
  - 修复后同时合并到main和develop
  - 合并后删除

---

## 💻 本地开发流程

### 阶段1: 准备工作

#### 1.1 同步最新代码
```bash
# 切换到main分支
git checkout main

# 拉取最新代码
git pull origin main

# 创建新的功能分支
git checkout -b feature/新功能名称_20251219
```

#### 1.2 检查环境
```bash
# 检查Python环境
python --version

# 检查依赖
pip list | grep -E "flask|sqlalchemy|psycopg2"

# 检查数据库连接
python -c "from database import get_session; print('DB OK')"
```

#### 1.3 备份数据库（如需要）
```bash
# 如果涉及数据库变更，先备份
pg_dump -h localhost -U your_user -d papers > backup_$(date +%Y%m%d_%H%M%S).sql
```

### 阶段2: 开发与测试

#### 2.1 编写代码
- 遵循项目代码规范
- 添加必要的注释
- 保持代码整洁

#### 2.2 本地测试
```bash
# 启动本地开发服务器
python app.py
# 或
flask run

# 测试新功能
# 1. 功能测试：验证功能是否正常工作
# 2. 边界测试：测试异常情况
# 3. 集成测试：测试与其他模块的交互
```

#### 2.3 代码检查
```bash
# 检查语法错误
python -m py_compile your_file.py

# 检查导入
python -c "import your_module"

# 运行测试（如果有）
pytest tests/
```

### 阶段3: 提交准备

#### 3.1 查看变更
```bash
# 查看所有变更
git status

# 查看具体变更内容
git diff

# 查看已暂存的变更
git diff --cached
```

#### 3.2 暂存变更
```bash
# 添加所有变更
git add .

# 或选择性添加
git add path/to/file1.py path/to/file2.py

# 添加新文件
git add new_file.py
```

#### 3.3 提交变更
```bash
# 提交（遵循提交信息规范）
git commit -m "功能: 添加follow研究机构功能

- 新增AllenAI、NVIDIA Research、Physical Intelligence追踪
- 新增定时任务自动抓取
- 新增前端展示页面
- 更新数据库模型

相关issue: #123"
```

**提交信息规范**:
```
类型: 简短描述（50字以内）

详细描述（可选）:
- 变更点1
- 变更点2

相关issue: #123
```

**提交类型**:
- `功能`: 新功能
- `修复`: Bug修复
- `文档`: 文档更新
- `重构`: 代码重构
- `优化`: 性能优化
- `样式`: 代码格式调整
- `测试`: 测试相关
- `配置`: 配置文件变更

---

## 🔄 GitHub版本管理流程

### 阶段1: 推送代码

#### 1.1 推送到远程分支
```bash
# 首次推送，创建远程分支
git push -u origin feature/新功能名称_20251219

# 后续推送
git push
```

#### 1.2 创建Pull Request
1. 访问GitHub仓库
2. 点击"New Pull Request"
3. 选择:
   - base: `main`
   - compare: `feature/新功能名称_20251219`
4. 填写PR描述:
   - 功能说明
   - 变更内容
   - 测试情况
   - 相关issue
5. 创建PR

### 阶段2: 代码审查

#### 2.1 自我审查清单
- [ ] 代码功能完整
- [ ] 本地测试通过
- [ ] 没有语法错误
- [ ] 没有敏感信息泄露
- [ ] 数据库变更已备份
- [ ] 配置文件变更已说明
- [ ] 文档已更新（如需要）

#### 2.2 审查PR
- 检查代码变更
- 检查测试结果
- 检查文档更新
- 确认合并策略

### 阶段3: 合并到main

#### 3.1 合并PR
- 点击"Merge pull request"
- 选择合并方式:
  - **Squash and merge**（推荐）: 将多个commit合并为一个
  - **Create a merge commit**: 保留所有commit历史
  - **Rebase and merge**: 线性历史

#### 3.2 删除功能分支
- 合并后删除远程分支
- 本地切换回main并删除功能分支:
```bash
git checkout main
git pull origin main
git branch -d feature/新功能名称_20251219
```

#### 3.3 打标签（重要版本）
```bash
# 切换到main并拉取最新代码
git checkout main
git pull origin main

# 打标签
git tag -a v1.2.0 -m "版本1.2.0: 添加follow研究机构功能"

# 推送标签
git push origin v1.2.0
```

---

## 🚀 服务器部署流程

### 阶段1: 部署前检查

#### 1.1 确认代码状态
```bash
# 在本地确认main分支最新
git checkout main
git pull origin main

# 查看最新commit
git log -1

# 确认要部署的版本
git show HEAD --stat
```

#### 1.2 检查服务器状态
```bash
# SSH连接到服务器
ssh user@your-server.com

# 检查当前运行状态
sudo systemctl status embodied-pulse

# 检查数据库连接
psql -h localhost -U your_user -d papers -c "SELECT COUNT(*) FROM papers;"

# 检查磁盘空间
df -h

# 检查内存使用
free -h
```

#### 1.3 备份生产环境
```bash
# 备份数据库
pg_dump -h localhost -U your_user -d papers > /backup/papers_$(date +%Y%m%d_%H%M%S).sql

# 备份代码（可选，GitHub已有备份）
cd /path/to/project
tar -czf /backup/code_backup_$(date +%Y%m%d_%H%M%S).tar.gz .

# 备份配置文件
cp .env /backup/env_backup_$(date +%Y%m%d_%H%M%S)
```

### 阶段2: 部署代码

#### 2.1 拉取最新代码
```bash
# 进入项目目录
cd /path/to/embodied-pulse

# 拉取最新代码
git fetch origin
git checkout main
git pull origin main

# 确认版本
git log -1
```

#### 2.2 更新依赖（如需要）
```bash
# 激活虚拟环境
source venv/bin/activate

# 更新依赖
pip install -r requirements.txt

# 检查依赖版本
pip list
```

#### 2.3 数据库迁移（如需要）
```bash
# 如果有数据库变更，执行迁移
python migrate_add_new_fields.py

# 验证迁移结果
psql -h localhost -U your_user -d papers -c "\d papers"
```

#### 2.4 重启服务
```bash
# 重启应用服务
sudo systemctl restart embodied-pulse

# 检查服务状态
sudo systemctl status embodied-pulse

# 查看日志
sudo journalctl -u embodied-pulse -f
```

### 阶段3: 部署后验证

#### 3.1 功能验证
- [ ] 访问首页，确认正常加载
- [ ] 测试登录功能
- [ ] 测试新功能（如果有）
- [ ] 测试关键功能（论文列表、视频列表等）
- [ ] 检查定时任务状态

#### 3.2 性能检查
```bash
# 检查服务响应时间
curl -w "@curl-format.txt" -o /dev/null -s https://your-domain.com/

# 检查数据库查询性能
psql -h localhost -U your_user -d papers -c "EXPLAIN ANALYZE SELECT * FROM papers LIMIT 10;"

# 检查系统资源
top
htop
```

#### 3.3 日志检查
```bash
# 查看应用日志
tail -f /path/to/logs/app.log

# 查看错误日志
grep -i error /path/to/logs/app.log | tail -20

# 查看Nginx日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

---

## 📝 完整工作流示例

### 场景: 添加新功能 "follow研究机构"

#### 步骤1: 本地开发
```bash
# 1. 同步最新代码
git checkout main
git pull origin main

# 2. 创建功能分支
git checkout -b feature/follow_research_institutes_20251219

# 3. 开发代码
# ... 编写代码 ...

# 4. 本地测试
python app.py
# 浏览器测试功能

# 5. 提交代码
git add .
git commit -m "功能: 添加follow研究机构功能

- 新增AllenAI、NVIDIA Research、Physical Intelligence追踪
- 新增定时任务自动抓取
- 新增前端展示页面
- 更新数据库模型"
```

#### 步骤2: 推送到GitHub
```bash
# 1. 推送分支
git push -u origin feature/follow_research_institutes_20251219

# 2. 在GitHub创建PR
# - base: main
# - compare: feature/follow_research_institutes_20251219
# - 填写PR描述

# 3. 自我审查
# - 检查代码
# - 检查测试
# - 检查文档

# 4. 合并PR
# - 选择"Squash and merge"
# - 确认合并
```

#### 步骤3: 服务器部署
```bash
# 1. SSH连接服务器
ssh user@your-server.com

# 2. 备份数据库
pg_dump -h localhost -U your_user -d papers > /backup/papers_$(date +%Y%m%d_%H%M%S).sql

# 3. 拉取最新代码
cd /path/to/embodied-pulse
git pull origin main

# 4. 更新依赖（如需要）
source venv/bin/activate
pip install -r requirements.txt

# 5. 数据库迁移（如需要）
python migrate_add_research_institutes.py

# 6. 重启服务
sudo systemctl restart embodied-pulse

# 7. 验证部署
sudo systemctl status embodied-pulse
tail -f /path/to/logs/app.log
```

#### 步骤4: 验证与监控
- 访问网站测试新功能
- 检查日志确认无错误
- 监控系统资源使用
- 确认定时任务正常运行

---

## ✅ 安全检查清单

### 部署前检查
- [ ] 代码已通过本地测试
- [ ] 代码已推送到GitHub
- [ ] PR已合并到main分支
- [ ] 数据库已备份
- [ ] 配置文件已检查（无敏感信息）
- [ ] 依赖更新已测试
- [ ] 数据库迁移脚本已测试

### 部署中检查
- [ ] 代码拉取成功
- [ ] 依赖安装成功
- [ ] 数据库迁移成功（如需要）
- [ ] 服务重启成功
- [ ] 服务状态正常

### 部署后检查
- [ ] 网站可正常访问
- [ ] 登录功能正常
- [ ] 新功能正常（如有）
- [ ] 关键功能正常
- [ ] 定时任务正常
- [ ] 日志无错误
- [ ] 性能正常

---

## 🔧 常见问题处理

### 问题1: 合并冲突
```bash
# 1. 拉取最新代码
git pull origin main

# 2. 解决冲突
# 编辑冲突文件，解决冲突标记

# 3. 标记已解决
git add resolved_file.py

# 4. 继续合并
git commit -m "解决合并冲突"
```

### 问题2: 部署后服务无法启动
```bash
# 1. 查看服务状态
sudo systemctl status embodied-pulse

# 2. 查看详细日志
sudo journalctl -u embodied-pulse -n 50

# 3. 检查配置文件
python -c "from app import app; print('Config OK')"

# 4. 检查数据库连接
python -c "from database import get_session; print('DB OK')"

# 5. 回滚到上一版本
git checkout HEAD~1
sudo systemctl restart embodied-pulse
```

### 问题3: 数据库迁移失败
```bash
# 1. 恢复数据库备份
psql -h localhost -U your_user -d papers < /backup/papers_YYYYMMDD_HHMMSS.sql

# 2. 检查迁移脚本
python migrate_script.py --dry-run

# 3. 修复迁移脚本后重试
python migrate_script.py
```

### 问题4: 代码推送到错误分支
```bash
# 1. 查看当前分支
git branch

# 2. 创建新分支保存当前代码
git branch backup_branch

# 3. 切换到正确分支
git checkout correct_branch

# 4. 合并代码
git merge backup_branch

# 5. 删除错误推送
git push origin --delete wrong_branch
```

---

## 🔙 回滚策略

### 快速回滚（代码回滚）

#### 方法1: Git回滚
```bash
# 1. 查看提交历史
git log --oneline -10

# 2. 回滚到指定commit
git checkout <commit_hash>

# 3. 强制推送（谨慎使用）
git push origin main --force
```

#### 方法2: 标签回滚
```bash
# 1. 查看标签
git tag -l

# 2. 回滚到指定标签
git checkout v1.1.0

# 3. 创建回滚分支
git checkout -b rollback_to_v1.1.0

# 4. 合并到main
git checkout main
git merge rollback_to_v1.1.0
```

### 数据库回滚
```bash
# 1. 恢复数据库备份
psql -h localhost -U your_user -d papers < /backup/papers_YYYYMMDD_HHMMSS.sql

# 2. 验证数据
psql -h localhost -U your_user -d papers -c "SELECT COUNT(*) FROM papers;"
```

### 完整回滚流程
```bash
# 1. 停止服务
sudo systemctl stop embodied-pulse

# 2. 回滚代码
cd /path/to/embodied-pulse
git checkout <previous_commit>
git pull origin main --force

# 3. 恢复数据库
psql -h localhost -U your_user -d papers < /backup/papers_YYYYMMDD_HHMMSS.sql

# 4. 重启服务
sudo systemctl start embodied-pulse

# 5. 验证回滚
sudo systemctl status embodied-pulse
curl https://your-domain.com/
```

---

## 📊 版本管理最佳实践

### 1. 版本号规范
使用语义化版本号: `主版本号.次版本号.修订号`
- **主版本号**: 不兼容的API修改
- **次版本号**: 向下兼容的功能性新增
- **修订号**: 向下兼容的问题修正

示例: `v1.2.3`

### 2. 标签管理
```bash
# 创建标签
git tag -a v1.2.0 -m "版本1.2.0: 添加follow研究机构功能"

# 推送标签
git push origin v1.2.0

# 查看标签
git tag -l

# 删除标签（谨慎）
git tag -d v1.2.0
git push origin --delete v1.2.0
```

### 3. 发布说明
每次重要版本发布，创建 `CHANGELOG.md` 或发布说明文档:
```markdown
# 版本 1.2.0 (2025-12-19)

## 新增功能
- 添加follow研究机构功能
- 支持追踪AllenAI、NVIDIA Research、Physical Intelligence

## 修复问题
- 修复B站数据更新问题
- 修复登录验证bug

## 优化
- 优化论文搜索性能
- 优化前端加载速度
```

---

## 🎯 总结

### 核心流程
1. **本地开发** → 测试 → 提交
2. **GitHub管理** → PR → 审查 → 合并
3. **服务器部署** → 备份 → 更新 → 验证

### 关键原则
- ✅ 永远保护生产环境
- ✅ 版本控制是唯一真相源
- ✅ 测试优先
- ✅ 可追溯性

### 安全措施
- ✅ 部署前备份
- ✅ 部署后验证
- ✅ 准备回滚方案
- ✅ 监控和日志

---

## 📚 附录

### A. 常用 Git 命令速查

```bash
# 分支操作
git branch                    # 查看本地分支
git branch -a                 # 查看所有分支
git checkout -b feature/xxx   # 创建并切换分支
git branch -d feature/xxx     # 删除本地分支

# 提交操作
git status                    # 查看状态
git add .                     # 添加所有修改
git commit -m "message"       # 提交
git log --oneline             # 查看提交历史

# 远程操作
git fetch origin              # 获取远程更新
git pull origin main          # 拉取并合并
git push origin branch-name   # 推送到远程

# 标签操作
git tag -a v1.0.0 -m "msg"   # 创建标签
git push origin v1.0.0       # 推送标签
```

### B. 服务器常用命令

```bash
# 服务管理
systemctl status embodiedpulse    # 查看服务状态
systemctl restart embodiedpulse   # 重启服务
systemctl stop embodiedpulse      # 停止服务
systemctl start embodiedpulse     # 启动服务

# Docker 管理
docker-compose ps                 # 查看容器状态
docker-compose logs -f web        # 查看日志
docker-compose restart web        # 重启容器
docker-compose down               # 停止所有容器
docker-compose up -d --build     # 构建并启动

# 文件操作
tail -f app.log                   # 实时查看日志
grep "ERROR" app.log              # 搜索错误日志
df -h                             # 查看磁盘空间
free -h                           # 查看内存使用
```

### C. 紧急联系信息

- **GitHub 仓库**：`https://github.com/aramisjiang-wq/EmbodiedPulse2026`
- **服务器地址**：`115.190.77.57:5001`
- **部署日志**：GitHub Actions 页面

### D. 相关文档

- [开发规范完整版](../11-开发规范/开发规范完整版_20251219_v2.0.md)
- [部署教程](../部署教程/本地到服务器配置变更完整指南_20251219_v1.0.md)
- [发版记录](../../05-开发记录/发版记录_CHANGELOG.md)

---

**最后更新**: 2025-12-19  
**维护者**: 开发团队  
**版本历史**:
- v2.0 (2025-12-19): 完整版，合并v1.0内容，添加附录
- v1.0 (2025-12-19): 初始版本

