# 服务器脚本命令清单

**版本**: v1.0  
**日期**: 2025-12-17  
**用途**: 整理所有可在服务器上使用的脚本命令，分类并说明用途

---

## 📋 目录

1. [部署类脚本](#一部署类脚本)
2. [服务管理类脚本](#二服务管理类脚本)
3. [配置类脚本](#三配置类脚本)
4. [检查诊断类脚本](#四检查诊断类脚本)
5. [数据管理类脚本](#五数据管理类脚本)
6. [Python工具脚本](#六python工具脚本)
7. [常用系统命令](#七常用系统命令)

---

## 一、部署类脚本

### 1.1 `scripts/deploy_server.sh` ⭐ **最重要**

**用途**: GitHub Actions 自动部署时调用的核心脚本

**执行位置**: 服务器（由 GitHub Actions 自动调用）

**功能**:
- 拉取最新代码（`git pull origin main`）
- 激活虚拟环境
- 更新 Python 依赖（`pip install -r requirements.txt`）
- 重启 systemd 服务

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
bash scripts/deploy_server.sh
```

**注意事项**:
- 此脚本由 GitHub Actions 自动调用，通常不需要手动执行
- 如果手动执行，确保在项目根目录

---

### 1.2 `scripts/create_systemd_service.sh`

**用途**: 创建 systemd 服务配置文件

**执行位置**: 服务器（首次部署时执行一次）

**功能**:
- 创建 `/etc/systemd/system/embodiedpulse.service` 服务文件
- 配置环境变量加载（`EnvironmentFile`）
- 启用服务（开机自启）

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
chmod +x scripts/create_systemd_service.sh
bash scripts/create_systemd_service.sh
```

**执行后**:
```bash
# 启动服务
systemctl start embodiedpulse

# 查看状态
systemctl status embodiedpulse
```

---

## 二、服务管理类脚本

### 2.1 `scripts/fix_port_and_service.sh`

**用途**: 修复端口占用和 systemd 服务配置

**执行位置**: 服务器

**功能**:
- 停止 `embodiedpulse` 服务
- 查找并杀死占用 5001 端口的进程
- 确保 systemd 服务配置包含 `EnvironmentFile`

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
chmod +x scripts/fix_port_and_service.sh
bash scripts/fix_port_and_service.sh
```

**适用场景**:
- 服务启动失败，提示端口被占用
- 环境变量未加载，定时任务未启动

---

### 2.2 `scripts/restart_server.sh`

**用途**: 重启服务器服务

**执行位置**: 服务器

**功能**:
- 重启 `embodiedpulse` 服务
- 重启 `nginx` 服务

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
bash scripts/restart_server.sh
```

---

## 三、配置类脚本

### 3.1 `scripts/nginx_config_fix.sh` ⭐ **重要**

**用途**: 配置 Nginx 反向代理（多域名路由）

**执行位置**: 服务器（首次部署和配置修改时）

**功能**:
- 创建 `/etc/nginx/sites-available/embodiedpulse.conf`
- 配置 4 个域名的路由规则：
  - `login.gradmotion.com` → `/login`
  - `essay.gradmotion.com` → `/`
  - `blibli.gradmotion.com` → `/bilibili`
  - `admin123.gradmotion.com` → `/admin`
- 配置静态资源和 API 路由
- 测试配置并重载 Nginx

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
chmod +x scripts/nginx_config_fix.sh
bash scripts/nginx_config_fix.sh
```

**执行后验证**:
```bash
# 测试配置语法
nginx -t

# 重载配置
systemctl reload nginx
```

---

### 3.2 `scripts/setup_https.sh` ⭐ **重要**

**用途**: 配置 HTTPS 证书（使用 Certbot）

**执行位置**: 服务器（DNS 解析生效后执行）

**功能**:
- 安装 Certbot
- 为 4 个域名申请 Let's Encrypt SSL 证书
- 自动配置 Nginx HTTPS
- 设置自动续期

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
chmod +x scripts/setup_https.sh
bash scripts/setup_https.sh
```

**前提条件**:
- DNS 解析必须已生效
- 80 端口必须开放（Let's Encrypt 验证需要）

**执行后验证**:
```bash
# 查看证书列表
certbot certificates

# 测试 HTTPS
curl -I https://essay.gradmotion.com
```

---

### 3.3 `scripts/check_and_fix_scheduler.sh`

**用途**: 检查和修复定时任务配置

**执行位置**: 服务器

**功能**:
- 检查 `.env` 文件中的定时任务配置
- 如果缺失或错误，自动设置默认值
- 确保 `AUTO_FETCH_ENABLED=true`

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
chmod +x scripts/check_and_fix_scheduler.sh
bash scripts/check_and_fix_scheduler.sh
```

**适用场景**:
- 定时任务未启动
- 环境变量配置丢失

---

## 四、检查诊断类脚本

### 4.1 `scripts/check_deployment_status.sh` ⭐ **推荐**

**用途**: 全面检查部署状态和上线准备情况

**执行位置**: 服务器

**功能**:
- 检查数据库文件
- 检查服务状态（systemd、Nginx）
- 检查端口监听
- 检查环境变量配置
- 检查定时任务状态
- 检查 Nginx 配置
- 检查 HTTPS 证书
- 检查 DNS 解析
- 检查代码和依赖
- 检查错误日志

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
chmod +x scripts/check_deployment_status.sh
bash scripts/check_deployment_status.sh
```

**输出示例**:
```
✓ 数据库文件存在
✓ 服务运行正常
✓ 端口监听正常
⚠️  环境变量缺失: AUTO_FETCH_ENABLED
...
```

---

### 4.2 `scripts/verify_deployment.sh`

**用途**: 验证部署是否成功

**执行位置**: 服务器

**功能**:
- 检查服务是否运行
- 测试本地访问（`http://127.0.0.1:5001`）
- 测试域名访问（4 个域名）
- 检查 API 端点

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
chmod +x scripts/verify_deployment.sh
bash scripts/verify_deployment.sh
```

---

### 4.3 `scripts/check_bilibili_update_system.py`

**用途**: 检查 B站数据更新系统

**执行位置**: 服务器

**功能**:
- 检查数据新鲜度
- 检查调度器配置
- 检查数据库数据
- 检查 API 端点

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
source venv/bin/activate
python3 scripts/check_bilibili_update_system.py
```

---

### 4.4 `scripts/diagnose_paper_fetch.py`

**用途**: 诊断论文抓取问题

**执行位置**: 服务器

**功能**:
- 检查 `.env` 配置
- 检查 APScheduler 状态
- 检查数据库论文数量
- 检查最新论文日期

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
source venv/bin/activate
python3 scripts/diagnose_paper_fetch.py
```

---

## 五、数据管理类脚本

### 5.1 `scripts/export_papers_to_csv.py`

**用途**: 导出论文数据到 CSV 文件

**执行位置**: 服务器或本地

**功能**:
- 连接数据库
- 查询所有论文记录
- 导出为 CSV 格式

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
source venv/bin/activate
python3 scripts/export_papers_to_csv.py
```

**输出文件**: `papers_export_YYYYMMDD_HHMMSS.csv`

---

## 六、Python工具脚本

### 6.1 `scripts/check_feishu_config.py`

**用途**: 检查飞书 OAuth 配置

**执行位置**: 服务器或本地

**功能**:
- 检查 `FEISHU_APP_ID`
- 检查 `FEISHU_APP_SECRET`
- 检查 `FEISHU_REDIRECT_URI`
- 分析回调地址格式

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
source venv/bin/activate
python3 scripts/check_feishu_config.py
```

---

### 6.2 `scripts/data_health_check.py`

**用途**: 数据健康检查

**执行位置**: 服务器或本地

**功能**:
- 检查各数据源的健康状态
- 检查数据更新频率
- 检查数据完整性

**使用方法**:
```bash
cd /srv/EmbodiedPulse2026
source venv/bin/activate
python3 scripts/data_health_check.py
```

---

## 七、常用系统命令

### 7.1 服务管理命令

```bash
# 启动服务
systemctl start embodiedpulse

# 停止服务
systemctl stop embodiedpulse

# 重启服务
systemctl restart embodiedpulse

# 查看状态
systemctl status embodiedpulse

# 查看日志（实时）
journalctl -u embodiedpulse -f

# 查看最近50条日志
journalctl -u embodiedpulse -n 50

# 启用开机自启
systemctl enable embodiedpulse

# 禁用开机自启
systemctl disable embodiedpulse
```

### 7.2 Nginx 管理命令

```bash
# 测试配置语法
nginx -t

# 重载配置（不中断服务）
systemctl reload nginx

# 重启 Nginx
systemctl restart nginx

# 查看错误日志
tail -f /var/log/nginx/error.log

# 查看访问日志
tail -f /var/log/nginx/access.log
```

### 7.3 端口检查命令

```bash
# 检查端口是否被占用
lsof -i:5001

# 或
netstat -tuln | grep 5001

# 杀死占用端口的进程
lsof -ti:5001 | xargs kill -9

# 或
fuser -k 5001/tcp
```

### 7.4 数据库操作命令

```bash
# 备份数据库
cp instance/papers.db backups/papers_$(date +%Y%m%d).db
cp bilibili.db backups/bilibili_$(date +%Y%m%d).db

# 检查数据库内容（SQLite）
sqlite3 instance/papers.db "SELECT COUNT(*) FROM papers;"
sqlite3 bilibili.db "SELECT COUNT(*) FROM bilibili_up;"

# 压缩备份
gzip backups/papers_*.db
```

### 7.5 环境变量检查命令

```bash
# 检查 .env 文件
cat /srv/EmbodiedPulse2026/.env

# 检查 systemd 服务配置
cat /etc/systemd/system/embodiedpulse.service | grep EnvironmentFile

# 检查环境变量是否加载
systemctl show embodiedpulse | grep Environment
```

### 7.6 代码更新命令

```bash
# 进入项目目录
cd /srv/EmbodiedPulse2026

# 拉取最新代码
git pull origin main

# 查看修改
git status

# 查看日志
git log --oneline -10
```

### 7.7 虚拟环境命令

```bash
# 激活虚拟环境
cd /srv/EmbodiedPulse2026
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 查看已安装包
pip list

# 退出虚拟环境
deactivate
```

### 7.8 文件权限命令

```bash
# 设置文件权限
chmod 644 instance/papers.db
chmod 644 bilibili.db

# 设置所有者
chown root:root instance/papers.db
chown root:root bilibili.db

# 设置脚本执行权限
chmod +x scripts/*.sh
```

---

## 八、脚本分类快速索引

### 首次部署必执行

1. ✅ `scripts/create_systemd_service.sh` - 创建服务
2. ✅ `scripts/nginx_config_fix.sh` - 配置 Nginx
3. ✅ `scripts/setup_https.sh` - 配置 HTTPS（DNS 生效后）

### 日常维护常用

1. ✅ `scripts/check_deployment_status.sh` - 全面检查
2. ✅ `scripts/restart_server.sh` - 重启服务
3. ✅ `scripts/deploy_server.sh` - 代码更新（GitHub Actions 自动调用）

### 故障排查常用

1. ✅ `scripts/fix_port_and_service.sh` - 修复端口占用
2. ✅ `scripts/check_and_fix_scheduler.sh` - 修复定时任务
3. ✅ `scripts/diagnose_paper_fetch.py` - 诊断论文抓取
4. ✅ `scripts/check_bilibili_update_system.py` - 诊断 B站更新

### 数据管理常用

1. ✅ `scripts/export_papers_to_csv.py` - 导出数据

---

## 九、注意事项

### 9.1 脚本执行权限

所有 `.sh` 脚本首次执行前需要添加执行权限：

```bash
chmod +x scripts/*.sh
```

### 9.2 Python 脚本执行

Python 脚本需要在虚拟环境中执行：

```bash
cd /srv/EmbodiedPulse2026
source venv/bin/activate
python3 scripts/xxx.py
```

### 9.3 脚本执行位置

- **部署类脚本**: 必须在项目根目录执行（`/srv/EmbodiedPulse2026`）
- **系统配置脚本**: 可以在任意位置执行（会自动定位项目目录）
- **Python 脚本**: 建议在项目根目录执行

### 9.4 备份重要数据

在执行可能修改数据的脚本前，建议先备份：

```bash
# 备份数据库
cp instance/papers.db backups/papers_backup_$(date +%Y%m%d_%H%M%S).db
cp bilibili.db backups/bilibili_backup_$(date +%Y%m%d_%H%M%S).db
```

---

**文档版本**: v1.0  
**最后更新**: 2025-12-17  
**维护者**: AI Assistant

