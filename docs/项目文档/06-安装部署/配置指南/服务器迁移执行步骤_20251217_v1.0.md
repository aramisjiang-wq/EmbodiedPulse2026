# 服务器迁移执行步骤

**版本**: v1.0  
**日期**: 2025-12-17

---

## 🚀 完整执行步骤

### 步骤1：SSH登录服务器

```bash
ssh root@101.200.222.139
# 输入密码: XLj4kUnh
```

### 步骤2：进入项目目录并拉取代码

```bash
cd /srv/EmbodiedPulse2026
git pull origin main
```

### 步骤3：执行迁移脚本

```bash
# 确保脚本有执行权限
chmod +x scripts/migrate_sqlite_to_postgresql_server.sh

# 执行迁移
bash scripts/migrate_sqlite_to_postgresql_server.sh
```

---

## ⚠️ 如果脚本不存在

如果拉取代码后脚本仍然不存在，可以手动执行以下命令：

```bash
cd /srv/EmbodiedPulse2026

# 1. 检查当前目录
pwd
ls -la scripts/

# 2. 如果scripts目录不存在或脚本不存在，强制拉取
git fetch origin
git reset --hard origin/main

# 3. 再次检查
ls -la scripts/migrate_sqlite_to_postgresql_server.sh
```

---

## 📝 一键执行命令（复制粘贴）

```bash
cd /srv/EmbodiedPulse2026 && git pull origin main && chmod +x scripts/migrate_sqlite_to_postgresql_server.sh && bash scripts/migrate_sqlite_to_postgresql_server.sh
```

---

**文档版本**: v1.0  
**最后更新**: 2025-12-17

