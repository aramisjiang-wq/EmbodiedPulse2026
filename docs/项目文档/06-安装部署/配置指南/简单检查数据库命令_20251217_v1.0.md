# 简单检查数据库命令

**版本**: v1.0  
**日期**: 2025-12-17

---

## 🔍 最简单的检查方法

### 方法1：检查.env文件

```bash
ssh root@101.200.222.139
cd /srv/EmbodiedPulse2026
cat .env | grep DATABASE_URL
```

### 方法2：检查SQLite文件

```bash
ssh root@101.200.222.139
cd /srv/EmbodiedPulse2026
ls -lh *.db
```

### 方法3：检查PostgreSQL

```bash
ssh root@101.200.222.139
sudo -u postgres psql -c "\l" | grep embodied
```

### 方法4：直接查询数据量（最简单）

```bash
ssh root@101.200.222.139
cd /srv/EmbodiedPulse2026
source venv/bin/activate

# 检查论文数据
python3 -c "from models import get_session, Paper; s=get_session(); print('论文:', s.query(Paper).count()); s.close()"

# 检查B站数据
python3 -c "from bilibili_models import get_bilibili_session, BilibiliUp, BilibiliVideo; s=get_bilibili_session(); print('UP主:', s.query(BilibiliUp).count()); print('视频:', s.query(BilibiliVideo).count()); s.close()"
```

---

**文档版本**: v1.0  
**最后更新**: 2025-12-17

