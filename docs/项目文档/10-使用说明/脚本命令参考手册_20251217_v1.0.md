# 脚本命令参考手册

**创建时间**：2025年12月10日  
**项目名称**：Robotics ArXiv Daily (Embodied Pulse)  
**版本**：v1.2.2  
**最后更新**：2025年12月10日  
**维护原则**：每次添加新脚本或修改脚本功能时，及时更新本文档

---

## 📋 目录

1. [数据抓取脚本](#数据抓取脚本)
2. [服务器管理脚本](#服务器管理脚本)
3. [数据库管理脚本](#数据库管理脚本)
4. [测试和诊断脚本](#测试和诊断脚本)
5. [数据维护脚本](#数据维护脚本)
6. [定时任务脚本](#定时任务脚本)
7. [部署脚本](#部署脚本)
8. [工具脚本](#工具脚本)

---

## 📥 数据抓取脚本

### 1. 抓取新数据（推荐）

**文件**：`fetch_new_data.py`  
**功能**：一键抓取论文和新闻数据

```bash
# 抓取论文和新闻（默认）
python3 fetch_new_data.py

# 只抓取论文
python3 fetch_new_data.py --papers

# 只抓取新闻
python3 fetch_new_data.py --news
```

**说明**：
- 自动配置参数（max_results=100, days_back=14）
- 启用去重和增量更新
- 启用Semantic Scholar数据获取
- 新闻抓取无需交互确认

---

### 2. 抓取论文数据

**文件**：`daily_arxiv.py`  
**功能**：从ArXiv抓取论文数据

```bash
# 方式1：使用配置文件（推荐）
python3 -c "
from daily_arxiv import load_config, demo
config = load_config('config.yaml')
config['max_results'] = 100
config['days_back'] = 14
demo(**config)
"

# 方式2：命令行运行
python3 daily_arxiv.py --config_path config.yaml

# 方式3：更新论文链接
python3 daily_arxiv.py --config_path config.yaml --update_paper_links
```

**参数说明**：
- `--config_path`: 配置文件路径（默认：config.yaml）
- `--update_paper_links`: 是否更新论文链接
- `max_results`: 每类最多抓取数量
- `days_back`: 抓取最近N天的论文
- `enable_dedup`: 是否启用去重
- `enable_incremental`: 是否启用增量更新
- `fetch_semantic_scholar`: 是否获取Semantic Scholar数据

---

### 3. 抓取新闻数据

**文件**：`fetch_news.py`  
**功能**：从多个新闻源抓取新闻

```bash
# 方式1：直接运行（需要交互确认）
python3 fetch_news.py

# 方式2：自动确认（无需交互）
python3 fetch_news.py --yes

# 方式3：使用fetch_new_data.py（推荐，无需交互）
python3 fetch_new_data.py --news
```

**参数说明**：
- `--yes`: 自动确认，不询问用户

**说明**：
- 从RSS、Orz.ai、NewsAPI等多个源抓取
- 只保存24小时内的新闻
- 自动去重

---

### 4. 抓取招聘信息

**文件**：`fetch_jobs.py`  
**功能**：从GitHub Jobs等抓取招聘信息

```bash
# 方式1：直接运行（需要交互确认）
n
# 方式2：自动确认（无需交互）
python3 fetch_jobs.py --yes
```

**参数说明**：
- `--yes`: 自动确认，不询问用户

**说明**：
- 从GitHub Jobs API抓取
- 自动保存到数据库

---

### 5. 补充论文数据

**文件**：`supplement_papers.py`  
**功能**：为指定类别补充论文数据（补充至100篇）

```bash
python3 supplement_papers.py
```

**说明**：
- 将所有标签的论文数量补充到至少100篇
- 自动检测每个类别的论文数量
- 只补充不足100篇的类别

---

### 6. 补充特定类别论文

**文件**：`supplement_specific.py`  
**功能**：为特定类别补充论文（RL/IL和Locomotion补充至300篇以上）

```bash
python3 supplement_specific.py
```

**说明**：
- 专门用于补充RL/IL和Locomotion类别
- 目标：每个类别至少300篇论文

---

### 7. 抓取特定日期论文

**文件**：`fetch_specific_dates.py`  
**功能**：抓取指定日期范围的论文

```bash
python3 fetch_specific_dates.py
```

**说明**：
- 用于抓取特定日期范围的论文
- 可以修改代码中的日期范围

---

## 🖥️ 服务器管理脚本

### 1. 启动Web服务器（基础）

**文件**：`start_web.sh`  
**功能**：启动Flask Web服务器

```bash
./start_web.sh
# 或
bash start_web.sh
```

**说明**：
- 启动Flask开发服务器
- 端口：5001
- 不包含定时任务

---

### 2. 启动服务器（带定时任务）

**文件**：`start_server_with_scheduler.sh`  
**功能**：启动服务器并启用定时任务

```bash
./start_server_with_scheduler.sh
# 或
bash start_server_with_scheduler.sh
```

**说明**：
- 启动Flask服务器
- 启用APScheduler定时任务
- 自动抓取数据

---

### 3. 启动服务器（带自动抓取）

**文件**：`start_with_auto_fetch.sh`  
**功能**：启动服务器并配置自动抓取

```bash
./start_with_auto_fetch.sh
# 或
bash start_with_auto_fetch.sh
```

**说明**：
- 设置环境变量启用自动抓取
- 默认每小时整点执行
- 使用Gunicorn运行（生产环境）

---

### 4. 重启服务器

**文件**：`restart_server.sh`  
**功能**：重启Flask服务器

```bash
./restart_server.sh
# 或
bash restart_server.sh
```

**说明**：
- 停止端口5001上的进程
- 重新启动Flask应用

---

### 5. 部署脚本

**文件**：`deploy.sh`  
**功能**：部署应用（传统服务器）

```bash
./deploy.sh
# 或
bash deploy.sh
```

**说明**：
- 更新代码（git pull）
- 安装/更新依赖
- 初始化数据库（如果需要）
- 重启服务

---

## 🗄️ 数据库管理脚本

### 1. 初始化数据库

**文件**：`init_database.py`  
**功能**：初始化数据库表结构

```bash
python3 init_database.py
```

**说明**：
- 创建所有数据库表
- 如果表已存在，不会重复创建
- 支持论文、新闻、招聘、数据集等数据库

---

### 2. 从JSON迁移到数据库

**文件**：`migrate_json_to_db.py`  
**功能**：将JSON文件数据迁移到数据库

```bash
python3 migrate_json_to_db.py
```

**说明**：
- 从`docs/cv-arxiv-daily.json`读取数据
- 迁移到SQLite数据库
- 自动去重

---

### 3. 添加Semantic Scholar字段

**文件**：`migrate_add_semantic_scholar_fields.py`  
**功能**：为数据库添加Semantic Scholar相关字段

```bash
python3 migrate_add_semantic_scholar_fields.py
```

**说明**：
- 添加citation_count等字段
- 数据库迁移脚本

---

## 🧪 测试和诊断脚本

### 1. 测试刷新API

**文件**：`test_refresh_api.py`  
**功能**：测试刷新全局数据API

```bash
python3 test_refresh_api.py
```

**说明**：
- 触发刷新API
- 轮询状态
- 显示详细执行结果
- 验证刷新功能是否正常

---

### 2. 诊断刷新功能

**文件**：`debug_refresh.py`  
**功能**：诊断刷新功能问题

```bash
python3 debug_refresh.py
```

**说明**：
- 检查配置文件
- 检查Python依赖
- 检查数据库连接
- 检查功能模块
- 检查网络连接
- 可选：实际测试抓取功能

---

### 3. 测试API接口

**文件**：`test_api.py`  
**功能**：测试API接口

```bash
python3 test_api.py
```

**说明**：
- 测试各个API端点
- 验证接口是否正常

---

### 4. 测试API Tube

**文件**：`test_apitube.py`  
**功能**：测试API Tube功能

```bash
python3 test_apitube.py
```

---

## 🔧 数据维护脚本

### 1. 检查重复数据

**文件**：`check_duplicates.py`  
**功能**：检查数据库中的重复论文

```bash
python3 check_duplicates.py
```

**说明**：
- 检查重复的论文ID
- 检查重复的标题
- 输出重复数据报告

---

### 2. 检查更新进度

**文件**：`check_update_progress.py`  
**功能**：检查数据更新进度

```bash
python3 check_update_progress.py
```

**说明**：
- 检查论文更新情况
- 显示更新统计

---

### 3. 修复新闻时间戳

**文件**：`fix_news_timestamps.py`  
**功能**：修复新闻时间戳问题

```bash
python3 fix_news_timestamps.py
```

**说明**：
- 修复新闻的发布时间戳
- 数据修复工具

---

### 4. 更新Semantic Scholar数据

**文件**：`update_semantic_scholar_data.py`  
**功能**：更新论文的Semantic Scholar数据

```bash
# 更新所有论文（跳过已有数据的）
python3 update_semantic_scholar_data.py

# 限制更新数量
python3 update_semantic_scholar_data.py --limit 200

# 强制更新已有数据
python3 update_semantic_scholar_data.py --skip-existing false
```

**参数说明**：
- `--limit`: 限制更新的论文数量（默认：None，更新所有）
- `--skip-existing`: 是否跳过已有数据的论文（默认：True）

**说明**：
- 批量更新论文的引用数等信息
- 从Semantic Scholar API获取数据
- 建议使用后台脚本运行（`run_semantic_update_background.sh`）

---

## ⏰ 定时任务脚本

### 1. 后台运行Semantic Scholar更新

**文件**：`run_semantic_update_background.sh`  
**功能**：在后台运行Semantic Scholar数据更新

```bash
./run_semantic_update_background.sh
# 或
bash run_semantic_update_background.sh
```

**说明**：
- 后台运行更新任务
- 不阻塞终端

---

### 2. 停止Semantic Scholar更新

**文件**：`stop_semantic_update.sh`  
**功能**：停止后台运行的Semantic Scholar更新

```bash
./stop_semantic_update.sh
# 或
bash stop_semantic_update.sh
```

---

### 3. 检查Semantic Scholar进度

**文件**：`check_semantic_progress.sh`  
**功能**：检查Semantic Scholar更新进度

```bash
./check_semantic_progress.sh
# 或
bash check_semantic_progress.sh
```

---

### 4. 监控更新任务

**文件**：`monitor_update.sh`  
**功能**：监控数据更新任务

```bash
./monitor_update.sh
# 或
bash monitor_update.sh
```

---

## 🚀 部署脚本

### 1. 部署脚本（传统服务器）

**文件**：`deploy.sh`  
**功能**：一键部署应用

```bash
./deploy.sh
# 或
bash deploy.sh
```

**执行步骤**：
1. 更新代码（git pull）
2. 激活虚拟环境
3. 安装/更新依赖
4. 初始化数据库（如果需要）
5. 重启服务

---

## 🛠️ 工具脚本

### 1. 保存数据集到数据库

**文件**：`save_datasets_to_db.py`  
**功能**：保存数据集信息到数据库

```bash
python3 save_datasets_to_db.py
```

---

### 2. 手动数据集数据

**文件**：`manual_datasets_data.py`  
**功能**：手动添加数据集数据

```bash
python3 manual_datasets_data.py
```

---

### 3. 掘金数据集解析器

**文件**：`juejin_datasets_parser.py`  
**功能**：解析掘金数据集数据

```bash
python3 juejin_datasets_parser.py
```

---

### 4. 具身新闻关键词

**文件**：`embodied_news_keywords.py`  
**功能**：处理具身新闻关键词

```bash
python3 embodied_news_keywords.py
```

---

### 5. 运势消息

**文件**：`fortune_messages.py`  
**功能**：生成运势消息

```bash
python3 fortune_messages.py
```

---

### 6. 重置运势（JavaScript）

**文件**：`reset_fortune.js`  
**功能**：重置运势数据

```bash
node reset_fortune.js
```

**说明**：
- 需要Node.js环境
- 重置前端运势数据

---

## 📊 脚本分类总结

### 按使用频率分类

**常用脚本**（日常使用）：
1. `fetch_new_data.py` - 抓取新数据
2. `restart_server.sh` - 重启服务器
3. `debug_refresh.py` - 诊断问题
4. `test_refresh_api.py` - 测试刷新功能

**维护脚本**（定期使用）：
1. `check_duplicates.py` - 检查重复数据
2. `update_semantic_scholar_data.py` - 更新Semantic Scholar数据
3. `check_update_progress.py` - 检查更新进度

**部署脚本**（部署时使用）：
1. `deploy.sh` - 部署应用
2. `start_with_auto_fetch.sh` - 启动生产环境

**开发脚本**（开发时使用）：
1. `test_api.py` - 测试API
2. `init_database.py` - 初始化数据库
3. `migrate_json_to_db.py` - 数据迁移

---

## 🔍 快速查找

### 按功能查找

| 功能 | 脚本 | 命令 |
|------|------|------|
| **抓取论文** | `fetch_new_data.py` | `python3 fetch_new_data.py --papers` |
| **抓取新闻** | `fetch_new_data.py` | `python3 fetch_new_data.py --news` |
| **抓取招聘** | `fetch_jobs.py` | `python3 fetch_jobs.py` |
| **启动服务器** | `start_with_auto_fetch.sh` | `./start_with_auto_fetch.sh` |
| **重启服务器** | `restart_server.sh` | `./restart_server.sh` |
| **诊断问题** | `debug_refresh.py` | `python3 debug_refresh.py` |
| **测试刷新** | `test_refresh_api.py` | `python3 test_refresh_api.py` |
| **初始化数据库** | `init_database.py` | `python3 init_database.py` |
| **检查重复** | `check_duplicates.py` | `python3 check_duplicates.py` |
| **部署应用** | `deploy.sh` | `./deploy.sh` |

---

## 📝 使用示例

### 示例1：日常数据更新

```bash
# 1. 抓取新数据
python3 fetch_new_data.py

# 2. 检查是否有重复数据
python3 check_duplicates.py

# 3. 重启服务器（如果需要）
./restart_server.sh
```

### 示例2：问题排查

```bash
# 1. 运行诊断脚本
python3 debug_refresh.py

# 2. 测试刷新API
python3 test_refresh_api.py

# 3. 查看服务器日志
docker-compose logs -f web
```

### 示例3：部署新版本

```bash
# 1. 部署应用
./deploy.sh

# 2. 验证部署
python3 test_refresh_api.py

# 3. 检查服务状态
curl http://localhost:5001/api/stats
```

---

## ⚠️ 注意事项

### 1. 权限问题

**Shell脚本**：
```bash
# 如果脚本没有执行权限
chmod +x script_name.sh
```

### 2. 环境变量

**某些脚本需要环境变量**：
```bash
# 设置环境变量
export AUTO_FETCH_ENABLED=true
export AUTO_FETCH_SCHEDULE="0 * * * *"
python3 app.py
```

### 3. 数据库备份

**执行数据操作前建议备份**：
```bash
# 备份数据库
cp papers.db papers.db.backup-$(date +%Y%m%d)
```

### 4. 依赖检查

**确保已安装所有依赖**：
```bash
pip install -r requirements.txt
```

---

## 🔄 更新记录

| 日期 | 版本 | 更新内容 | 更新人 |
|------|------|---------|--------|
| 2025-12-10 | v1.0 | 初始版本，整理所有脚本 | AI开发助手 |

---

## 📚 相关文档

- [生产环境部署方案](./生产环境部署方案_20251210.md)
- [定时任务配置说明](./定时任务配置说明_20251210.md)
- [刷新功能确认](./../04-功能说明/刷新功能确认_20251210.md)
- [刷新数据失败排查指南](./../08-问题修复/功能问题/刷新数据失败排查指南_20251210.md)

---

**最后更新**：2025年12月10日  
**维护者**：开发团队  
**更新原则**：每次添加新脚本或修改脚本功能时，及时更新本文档





