# 最终诊断：数据库值为0

**关键发现**：
- 直接SQL查询返回：`videos_count=0, views_count=0`
- ORM查询返回：`videos_count=0, views_count=0`
- **两者都返回0，说明数据库中这些字段确实是0！**

但之前独立查询显示有值（videos_count=49, views_count=3307108），可能是：
1. 查询了不同的数据库
2. 查询了不同的记录（可能有多个"逐际动力"的记录）

---

## 立即检查

在服务器上执行：

```bash
cd /srv/EmbodiedPulse2026

# 拉取最新代码
git pull origin main

# 运行检查脚本，查看所有"逐际动力"的记录
venv/bin/python3 scripts/check_bilibili_db_values.py
```

这个脚本会：
1. 用直接SQL查询所有名为"逐际动力"的记录
2. 用ORM查询所有名为"逐际动力"的记录
3. 特别查询is_active=True的记录

---

## 如果确实有多个记录

可能需要：
1. 检查是否有重复记录
2. 确认哪个记录是活跃的
3. 更新正确的记录

---

**文档结束**

