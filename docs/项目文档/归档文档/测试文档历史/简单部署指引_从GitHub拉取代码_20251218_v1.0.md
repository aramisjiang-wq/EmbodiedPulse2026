# 简单部署指引 - 从GitHub拉取代码

**版本**: v1.0  
**日期**: 2025-12-18  
**适用场景**: 本地代码已提交到GitHub，需要在服务器上拉取最新代码

---

## 快速部署（3步）

### 步骤1：本地提交代码到GitHub

在本地终端执行：

```bash
cd "/Users/dong/Documents/Cursor/Embodied Pulse"

# 查看修改的文件
git status

# 添加修改的文件
git add app.py bilibili_models.py templates/bilibili.html

# 提交
git commit -m "修复总播放数显示为0的问题"

# 推送到GitHub
git push origin main
```

### 步骤2：SSH登录服务器

```bash
ssh root@iZ2zeb3oa5878sn6lgjey2Z
```

### 步骤3：在服务器上拉取代码并重启

```bash
cd /srv/EmbodiedPulse2026

# 拉取最新代码
git pull origin main

# 重启应用
sudo systemctl restart embodiedpulse

# 检查状态
sudo systemctl status embodiedpulse
```

---

## 完整流程（带检查）

### 1. 本地提交代码

```bash
cd "/Users/dong/Documents/Cursor/Embodied Pulse"

# 检查修改
git status

# 添加文件
git add app.py bilibili_models.py templates/bilibili.html

# 提交
git commit -m "修复总播放数显示为0的问题（不修改数据库，API层处理）"

# 推送
git push origin main
```

### 2. 服务器拉取代码

```bash
# SSH登录服务器
ssh root@iZ2zeb3oa5878sn6lgjey2Z

# 进入项目目录
cd /srv/EmbodiedPulse2026

# 检查当前状态
git status

# 拉取最新代码
git pull origin main

# 如果有冲突，先备份本地修改
# git stash
# git pull origin main
```

### 3. 重启应用

```bash
# 重启服务
sudo systemctl restart embodiedpulse

# 等待3秒
sleep 3

# 检查服务状态
sudo systemctl status embodiedpulse
```

### 4. 验证修复

```bash
# 测试API返回
curl 'http://localhost:5001/api/bilibili/all?force=1' | python3 -m json.tool | grep -A 3 "user_stat"
```

应该看到：
```json
"user_stat": {
    "videos": "49",
    "views": "330.7万",  // ✅ 应该是格式化后的值，不是"0"
    "likes": "0"
}
```

---

## 一键部署脚本（可选）

如果经常需要部署，可以使用项目中的脚本：

```bash
# 在服务器上执行
cd /srv/EmbodiedPulse2026
bash scripts/fix_git_conflict_and_deploy.sh
```

这个脚本会：
1. 自动备份本地修改（如果有）
2. 拉取最新代码
3. 重启服务
4. 检查服务状态

---

## 常见问题

### Q1: Git pull 失败，提示有冲突

**解决**：
```bash
# 备份本地修改
git stash

# 拉取最新代码
git pull origin main

# 如果需要恢复本地修改
git stash pop
```

### Q2: 服务重启失败

**检查日志**：
```bash
sudo journalctl -u embodiedpulse -n 50 --no-pager
```

### Q3: 代码已更新但页面还是显示0

**解决**：
1. 清除浏览器缓存（Ctrl+Shift+R 或 Cmd+Shift+R）
2. 检查API返回：`curl 'http://localhost:5001/api/bilibili/all?force=1' | python3 -m json.tool | grep "views"`

---

## 总结

**最简单的3步**：
1. 本地：`git add . && git commit -m "修复" && git push`
2. 服务器：`ssh root@...` 然后 `cd /srv/EmbodiedPulse2026 && git pull`
3. 重启：`sudo systemctl restart embodiedpulse`

**完成！** 🎉

---

**文档结束**

