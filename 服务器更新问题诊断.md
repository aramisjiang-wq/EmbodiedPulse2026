# 服务器更新问题诊断

## 🔍 问题分析

### 当前状态确认

✅ **本地代码已修改**: `templates/bilibili.html` 包含最新优化
✅ **本地已提交**: 提交 `393f66b0`
❓ **GitHub推送状态**: 需要确认
❓ **服务器更新状态**: 未更新

## 🚨 可能的原因

### 1. GitHub推送可能失败（最可能）

虽然之前显示 "Everything up-to-date"，但可能是：
- 网络问题导致推送失败
- GitHub连接超时
- 认证问题

### 2. GitHub Actions自动部署未触发

可能原因：
- `templates/bilibili.html` 不在 `paths-ignore` 列表中，应该会触发
- 但可能GitHub Actions配置有问题
- 或者部署失败了

### 3. 服务器部署方式不匹配

发现有两个部署配置：
- `deploy.yml`: 使用 `/srv/EmbodiedPulse2026` + systemd
- `auto-deploy.yml`: 使用 `/opt/EmbodiedPulse` + Docker

服务器可能使用的是其中一个，但GitHub Actions可能没有正确执行。

## ✅ 解决方案

### 步骤1: 确认GitHub推送状态

```bash
# 检查本地和远程的差异
cd "/Users/dong/Documents/Cursor/Embodied Pulse"
git fetch origin
git log HEAD..origin/main --oneline

# 如果有输出，说明本地有未推送的提交
# 如果没有输出，说明已经同步
```

### 步骤2: 强制推送到GitHub

如果确认未推送，执行：

```bash
cd "/Users/dong/Documents/Cursor/Embodied Pulse"
git push origin main

# 如果失败，尝试：
git push origin main --force-with-lease
```

### 步骤3: 检查GitHub Actions

访问：https://github.com/aramisjiang-wq/EmbodiedPulse2026/actions

查看：
- 是否有最新的workflow运行记录
- 如果有，查看是否成功
- 如果失败，查看错误日志

### 步骤4: 手动触发部署

如果自动部署失败，可以手动触发：

1. **通过GitHub Actions界面**:
   - 访问 Actions 页面
   - 选择 "Auto Deploy to Server" workflow
   - 点击 "Run workflow" 按钮

2. **手动SSH到服务器**:
   ```bash
   ssh root@essay.gradmotion.com
   
   # 根据实际部署方式选择：
   # 方式1: systemd服务
   cd /srv/EmbodiedPulse2026
   git fetch origin
   git checkout main
   git pull origin main
   systemctl restart embodiedpulse
   
   # 方式2: Docker
   cd /opt/EmbodiedPulse
   git fetch origin
   git checkout main
   git pull origin main
   docker-compose restart web
   ```

## 🔧 快速诊断命令

### 检查GitHub推送状态

```bash
cd "/Users/dong/Documents/Cursor/Embodied Pulse"

# 方法1: 检查远程引用
git ls-remote origin main

# 方法2: 比较本地和远程
git fetch origin
git log --oneline --graph --decorate --all -5

# 方法3: 检查是否有未推送的提交
git log origin/main..HEAD --oneline
```

### 检查服务器状态（需要SSH）

```bash
# 检查服务器最新提交
ssh root@essay.gradmotion.com 'cd /srv/EmbodiedPulse2026 && git log --oneline -3'

# 检查文件是否更新
ssh root@essay.gradmotion.com 'cd /srv/EmbodiedPulse2026 && grep -c "年度播放量挂件高度 - 优化" templates/bilibili.html'
```

## 📝 建议操作流程

1. **立即检查**: 运行 `git fetch origin && git log origin/main..HEAD` 确认是否有未推送的提交
2. **如果未推送**: 执行 `git push origin main` 推送代码
3. **检查GitHub Actions**: 访问Actions页面查看部署状态
4. **如果部署失败**: 手动SSH到服务器更新代码
5. **验证更新**: 访问 https://essay.gradmotion.com/bilibili 检查效果

## 🎯 验证更新成功

更新成功后，应该看到：

1. **Git提交**: 服务器最新提交是 `393f66b0`
2. **文件内容**: `templates/bilibili.html` 包含 "年度播放量挂件高度 - 优化"
3. **网页效果**: 
   - 年度播放量图表高度是280px（不是350px）
   - "统计方式"文字完整显示
   - 标题与图表间距更紧凑

