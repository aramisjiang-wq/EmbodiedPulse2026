# 检查服务器更新状态

## 📋 当前状态

- ✅ **本地提交成功**: `393f66b0` - "fix(ui): 优化Bilibili页面右侧挂件布局和溢出问题"
- ✅ **GitHub推送成功**: 代码已推送到 `origin/main`
- ⏳ **服务器更新**: 需要检查

## 🔍 检查方法

### 方法1: 检查GitHub Actions自动部署（如果已配置）

访问GitHub Actions页面查看部署状态：
```
https://github.com/aramisjiang-wq/EmbodiedPulse2026/actions
```

如果看到最新的workflow运行记录，说明自动部署已触发。

### 方法2: SSH连接到服务器检查

#### 如果使用 systemd 服务（/srv/EmbodiedPulse2026）

```bash
# SSH连接到服务器
ssh root@essay.gradmotion.com

# 进入项目目录
cd /srv/EmbodiedPulse2026

# 检查最新提交
git log --oneline -3

# 检查是否需要更新
git fetch origin main
git log --oneline HEAD..origin/main

# 如果看到我们的提交 393f66b0，说明需要更新
# 更新代码
git pull origin main

# 重启服务
systemctl restart embodiedpulse

# 检查服务状态
systemctl status embodiedpulse
```

#### 如果使用 Docker（/opt/EmbodiedPulse）

```bash
# SSH连接到服务器
ssh root@essay.gradmotion.com

# 进入项目目录
cd /opt/EmbodiedPulse

# 检查最新提交
git log --oneline -3

# 检查是否需要更新
git fetch origin main
git log --oneline HEAD..origin/main

# 如果看到我们的提交 393f66b0，说明需要更新
# 更新代码
git pull origin main

# 重启Docker容器
docker-compose restart web

# 检查容器状态
docker-compose ps
docker-compose logs --tail=20 web
```

### 方法3: 使用检查脚本（本地运行）

```bash
# 在本地运行检查脚本
bash scripts/check_server_update.sh
```

### 方法4: 直接检查网页

访问服务器上的Bilibili页面，检查：
1. 右侧挂件是否正常显示
2. 年度播放量挂件的"统计方式"文字是否完整显示
3. 查看页面源代码，检查是否有最新的CSS样式

## ✅ 验证更新成功的标志

更新成功后，应该看到：

1. **Git提交**: 服务器上的最新提交是 `393f66b0`
2. **文件修改时间**: `templates/bilibili.html` 的修改时间是今天
3. **CSS样式**: 文件中包含以下内容：
   - `年度播放量挂件高度 - 优化`
   - `height: 280px` (年度图表)
   - `margin-bottom: 12px` (侧边栏标题区域)

## 🚀 如果服务器未更新，手动更新

### 方式1: 使用部署脚本

```bash
ssh root@essay.gradmotion.com
cd /srv/EmbodiedPulse2026
bash scripts/deploy_server.sh
```

### 方式2: 手动更新

```bash
ssh root@essay.gradmotion.com
cd /srv/EmbodiedPulse2026  # 或 /opt/EmbodiedPulse
git fetch origin
git checkout main
git pull origin main
systemctl restart embodiedpulse  # 或 docker-compose restart web
```

## 📝 检查清单

- [ ] GitHub Actions部署状态（如果有）
- [ ] 服务器最新Git提交
- [ ] 文件修改时间
- [ ] 服务运行状态
- [ ] 网页显示效果

